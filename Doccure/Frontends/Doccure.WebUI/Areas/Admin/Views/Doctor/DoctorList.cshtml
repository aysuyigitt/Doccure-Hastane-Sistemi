@model List<ResultDoctorDto>

@{
    ViewData["Title"] = "DoctorList";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";

    // Branch category mapping — BranchId'ye göre kategori belirlemek için
    // Bu mapping'i kendi BranchId'lerinize göre güncelleyin
    string GetCategory(string branchId) => branchId switch
    {
        "kardiyoloji" or "dermatoloji" or "noroloji" or "pediatri" => "dahili",
        "ortopedi" or "genel-cerrahi" or "travmatoloji" => "cerrahi",
        "radyoloji" or "patoloji" or "labaratuvar" => "tani",
        _ => "dahili"
    };

    // İsim baş harflerini al
    string GetInitials(string name, string surname)
        => $"{(name?.Length > 0 ? name[0] : ' ')}{(surname?.Length > 0 ? surname[0] : ' ')}".Trim().ToUpper();

    // Deneyime göre renk
    string GetAvatarGradient(int index) => (index % 8) switch
    {
        0 => "linear-gradient(135deg,#3B82F6,#1D4ED8)",
        1 => "linear-gradient(135deg,#10B981,#059669)",
        2 => "linear-gradient(135deg,#F59E0B,#D97706)",
        3 => "linear-gradient(135deg,#7C3AED,#6D28D9)",
        4 => "linear-gradient(135deg,#06B6D4,#0891B2)",
        5 => "linear-gradient(135deg,#6366F1,#4F46E5)",
        6 => "linear-gradient(135deg,#64748B,#475569)",
        7 => "linear-gradient(135deg,#EC4899,#DB2777)",
        _ => "linear-gradient(135deg,#3B82F6,#1D4ED8)"
    };
}

<main id="app-content">

    <style>
        /* ── Stat Grid ── */
        .dc-stat-grid {
            display: grid;
            grid-template-columns: repeat(4,1fr);
            gap: 14px;
            margin-bottom: 24px;
        }

        @@media(max-width:900px) {
            .dc-stat-grid {
                grid-template-columns: repeat(2,1fr)
            }
        }

        @@media(max-width:480px) {
            .dc-stat-grid {
                grid-template-columns: 1fr 1fr;
                gap: 10px
            }
        }

        .dc-stat-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 18px 20px;
            display: flex;
            align-items: center;
            gap: 14px;
            box-shadow: var(--shadow);
            transition: box-shadow var(--dur) var(--ease), transform var(--dur) var(--ease);
        }

            .dc-stat-card:hover {
                box-shadow: 0 4px 20px rgba(59,130,246,.13);
                transform: translateY(-2px);
            }

        .dc-si {
            width: 44px;
            height: 44px;
            border-radius: 12px;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 19px;
        }

        .dc-si-blue {
            background: var(--primary-bg);
            color: var(--primary);
        }

        .dc-si-green {
            background: var(--success-bg);
            color: var(--success);
        }

        .dc-si-orange {
            background: var(--warning-bg);
            color: var(--warning);
        }

        .dc-si-red {
            background: var(--danger-bg);
            color: var(--danger);
        }

        .dc-stat-val {
            font-size: 22px;
            font-weight: 700;
            color: var(--text);
            line-height: 1.1;
        }

        .dc-stat-lbl {
            font-size: 11px;
            color: var(--text-muted);
            font-weight: 500;
            margin-top: 2px;
        }

        .dc-stat-sub {
            font-size: 11px;
            font-weight: 600;
            margin-top: 4px;
        }

            .dc-stat-sub.up {
                color: var(--success);
            }

            .dc-stat-sub.down {
                color: var(--danger);
            }

        /* ── Toolbar ── */
        .dc-toolbar {
            display: flex;
            align-items: center;
            gap: 10px;
            flex-wrap: wrap;
            margin-bottom: 20px;
        }

        .dc-local-search {
            position: relative;
            flex: 1;
            min-width: 160px;
            max-width: 280px;
        }

            .dc-local-search i {
                position: absolute;
                left: 11px;
                top: 50%;
                transform: translateY(-50%);
                color: var(--text-muted);
                font-size: 13px;
                pointer-events: none;
            }

            .dc-local-search input {
                width: 100%;
                border: 1px solid var(--border);
                border-radius: 20px;
                padding: 8px 14px 8px 32px;
                font-size: 13px;
                font-family: inherit;
                background: var(--surface);
                color: var(--text);
                outline: none;
                transition: border-color var(--dur), box-shadow var(--dur);
            }

                .dc-local-search input:focus {
                    border-color: var(--primary);
                    box-shadow: 0 0 0 3px rgba(59,130,246,.1);
                }

                .dc-local-search input::placeholder {
                    color: var(--text-muted);
                }

        .dc-tabs {
            display: flex;
            gap: 5px;
            flex-wrap: wrap;
        }

        .dc-tab {
            border: 1px solid var(--border);
            background: var(--surface);
            border-radius: 20px;
            padding: 7px 15px;
            font-size: 12px;
            font-weight: 600;
            color: var(--text-muted);
            cursor: pointer;
            transition: all var(--dur) var(--ease);
            white-space: nowrap;
        }

            .dc-tab:hover {
                background: var(--primary-bg);
                color: var(--primary);
                border-color: #C7D9F8;
            }

            .dc-tab.active {
                background: var(--primary);
                color: #fff;
                border-color: var(--primary);
            }

        .dc-sort-select {
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            padding: 7px 12px;
            font-size: 12px;
            font-family: inherit;
            background: var(--surface);
            color: var(--text);
            outline: none;
            cursor: pointer;
        }

        .dc-view-toggle {
            display: flex;
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            overflow: hidden;
            flex-shrink: 0;
        }

        .dc-vbtn {
            width: 34px;
            height: 34px;
            border: none;
            background: var(--surface);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            color: var(--text-muted);
            cursor: pointer;
            transition: all var(--dur);
        }

            .dc-vbtn.active {
                background: var(--primary-bg);
                color: var(--primary);
            }

        /* ── Card Grid ── */
        #dc-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px,1fr));
            gap: 18px;
        }

            #dc-grid.list-mode {
                grid-template-columns: 1fr;
            }

        @@keyframes dcFadeUp {
            from {
                opacity: 0;
                transform: translateY(16px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        .dc-card {
            animation: dcFadeUp .3s var(--ease) both;
        }

            .dc-card:nth-child(1) {
                animation-delay: .04s
            }

            .dc-card:nth-child(2) {
                animation-delay: .08s
            }

            .dc-card:nth-child(3) {
                animation-delay: .12s
            }

            .dc-card:nth-child(4) {
                animation-delay: .16s
            }

            .dc-card:nth-child(5) {
                animation-delay: .20s
            }

            .dc-card:nth-child(6) {
                animation-delay: .24s
            }

            .dc-card:nth-child(7) {
                animation-delay: .28s
            }

            .dc-card:nth-child(8) {
                animation-delay: .32s
            }

        /* Card */
        .dc-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            display: flex;
            flex-direction: column;
            overflow: hidden;
            transition: box-shadow var(--dur) var(--ease), transform var(--dur) var(--ease), border-color var(--dur);
            position: relative;
        }

            .dc-card:hover {
                box-shadow: 0 4px 24px rgba(59,130,246,.13);
                transform: translateY(-3px);
                border-color: #C7D9F8;
            }

        .dc-card-top {
            padding: 22px 20px 16px;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            gap: 10px;
            position: relative;
        }

        .dc-card-top-bg {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 70px;
            opacity: .07;
            border-radius: var(--radius) var(--radius) 0 0;
        }

        /* Status badge */
        .dc-status-badge {
            position: absolute;
            top: 14px;
            right: 14px;
            font-size: 10px;
            font-weight: 700;
            padding: 3px 9px;
            border-radius: 20px;
            display: flex;
            align-items: center;
            gap: 4px;
        }

            .dc-status-badge::before {
                content: '';
                width: 6px;
                height: 6px;
                border-radius: 50%;
                flex-shrink: 0;
            }

            .dc-status-badge.on {
                background: var(--success-bg);
                color: #065F46;
            }

                .dc-status-badge.on::before {
                    background: var(--success);
                }

            .dc-status-badge.off {
                background: var(--warning-bg);
                color: #92400E;
            }

                .dc-status-badge.off::before {
                    background: var(--warning);
                }

            .dc-status-badge.leave {
                background: #F1F5F9;
                color: var(--text-muted);
            }

                .dc-status-badge.leave::before {
                    background: #94A3B8;
                }

        /* Avatar */
        .dc-avatar {
            width: 72px;
            height: 72px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            font-weight: 700;
            color: #fff;
            border: 3px solid var(--surface);
            box-shadow: 0 4px 14px rgba(0,0,0,.12);
            flex-shrink: 0;
            position: relative;
            z-index: 1;
            overflow: hidden;
        }

            .dc-avatar img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                border-radius: 50%;
            }

        .dc-avatar-ring {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 4px;
            position: relative;
            z-index: 1;
        }

        .dc-doc-name {
            font-size: 15px;
            font-weight: 700;
            color: var(--text);
            line-height: 1.3;
        }

        .dc-doc-spec {
            font-size: 12px;
            color: var(--text-muted);
            font-weight: 500;
        }

        .dc-doc-dept {
            font-size: 11px;
            font-weight: 600;
            padding: 3px 10px;
            border-radius: 20px;
            background: var(--primary-bg);
            color: var(--primary);
        }

        /* Quick info chips (price + exp) */
        .dc-chips {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
            justify-content: center;
            margin-top: 2px;
        }

        .dc-chip {
            font-size: 11px;
            font-weight: 600;
            padding: 3px 9px;
            border-radius: 20px;
            border: 1px solid var(--border);
            background: var(--bg);
            color: var(--text-muted);
            display: flex;
            align-items: center;
            gap: 4px;
        }

        /* Stats row */
        .dc-card-stats {
            display: flex;
            border-top: 1px solid var(--border);
            border-bottom: 1px solid var(--border);
        }

        .dc-cs-cell {
            flex: 1;
            text-align: center;
            padding: 10px 4px;
            border-right: 1px solid var(--border);
            background: var(--bg);
            transition: background var(--dur);
        }

            .dc-cs-cell:last-child {
                border-right: none;
            }

            .dc-cs-cell:hover {
                background: var(--primary-bg);
            }

        .dc-cs-val {
            font-size: 14px;
            font-weight: 700;
            color: var(--text);
            line-height: 1.1;
        }

        .dc-cs-lbl {
            font-size: 10px;
            color: var(--text-muted);
            font-weight: 500;
            margin-top: 2px;
        }

        /* Specializations */
        .dc-specs {
            padding: 12px 18px 14px;
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .dc-specs-title {
            font-size: 10.5px;
            font-weight: 700;
            letter-spacing: .06em;
            text-transform: uppercase;
            color: var(--text-muted);
            margin-bottom: 4px;
        }

        .dc-spec-row {
            display: flex;
            gap: 5px;
            flex-wrap: wrap;
        }

        .dc-spec-tag {
            font-size: 11px;
            font-weight: 600;
            padding: 4px 10px;
            border-radius: 6px;
            border: 1px solid var(--border);
            background: var(--bg);
            color: var(--text-muted);
        }

        /* Footer */
        .dc-card-footer {
            padding: 11px 14px;
            border-top: 1px solid var(--border);
            background: #FAFBFE;
            display: flex;
            gap: 6px;
            margin-top: auto;
        }

        .dc-btn {
            flex: 1;
            border: 1px solid var(--border);
            background: var(--surface);
            border-radius: var(--radius-sm);
            padding: 7px 8px;
            font-size: 12px;
            font-weight: 600;
            font-family: inherit;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 5px;
            cursor: pointer;
            transition: all var(--dur) var(--ease);
            color: var(--text-muted);
        }

            .dc-btn:hover {
                background: var(--bg);
                color: var(--text);
            }

            .dc-btn.primary {
                color: var(--primary);
                border-color: #C7D9F8;
                background: var(--primary-bg);
            }

                .dc-btn.primary:hover {
                    background: var(--primary);
                    color: #fff;
                    box-shadow: 0 3px 10px rgba(59,130,246,.25);
                }

            .dc-btn.success {
                color: var(--success);
                border-color: #A7F3D0;
                background: var(--success-bg);
            }

                .dc-btn.success:hover {
                    background: var(--success);
                    color: #fff;
                    box-shadow: 0 3px 10px rgba(16,185,129,.25);
                }

            .dc-btn.danger:hover {
                background: var(--danger-bg);
                border-color: #FCA5A5;
                color: var(--danger);
            }

        /* ── LIST MODE ── */
        #dc-grid.list-mode .dc-card {
            flex-direction: row;
            align-items: stretch;
        }

        #dc-grid.list-mode .dc-card-top {
            flex-direction: row;
            text-align: left;
            align-items: center;
            padding: 14px 20px;
            gap: 14px;
            min-width: 260px;
            flex-shrink: 0;
            border-right: 1px solid var(--border);
        }

        #dc-grid.list-mode .dc-card-top-bg {
            display: none;
        }

        #dc-grid.list-mode .dc-status-badge {
            position: static;
        }

        #dc-grid.list-mode .dc-avatar-ring {
            width: 52px;
            height: 52px;
            padding: 3px;
        }

        #dc-grid.list-mode .dc-avatar {
            width: 46px;
            height: 46px;
            font-size: 16px;
        }

        #dc-grid.list-mode .dc-chips {
            display: none;
        }

        #dc-grid.list-mode .dc-card-stats {
            flex: 1;
            border-top: none;
            border-bottom: none;
            border-right: 1px solid var(--border);
            flex-direction: column;
        }

        #dc-grid.list-mode .dc-cs-cell {
            border-right: none;
            border-bottom: 1px solid var(--border);
            padding: 8px 16px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            text-align: left;
        }

            #dc-grid.list-mode .dc-cs-cell:last-child {
                border-bottom: none;
            }

        #dc-grid.list-mode .dc-cs-lbl {
            font-size: 11px;
        }

        #dc-grid.list-mode .dc-specs {
            display: none;
        }

        #dc-grid.list-mode .dc-card-footer {
            flex-direction: column;
            border-top: none;
            border-left: none;
            padding: 12px;
            min-width: 120px;
            gap: 5px;
            justify-content: center;
        }

        #dc-grid.list-mode .dc-btn {
            flex: unset;
            width: 100%;
        }

        /* ── Empty ── */
        .dc-empty {
            grid-column: 1/-1;
            text-align: center;
            padding: 72px 24px;
            color: var(--text-muted);
        }

            .dc-empty i {
                font-size: 48px;
                opacity: .15;
                display: block;
                margin-bottom: 14px;
            }

        .dc-empty-title {
            font-size: 15px;
            font-weight: 700;
            color: var(--text);
            margin-bottom: 6px;
        }

        /* ── Modals ── */
        .dc-overlay {
            position: fixed;
            inset: 0;
            background: rgba(15,23,42,.45);
            z-index: 500;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 16px;
            opacity: 0;
            pointer-events: none;
            transition: opacity .2s ease;
        }

            .dc-overlay.show {
                opacity: 1;
                pointer-events: all;
            }

        .dc-modal-box {
            background: var(--surface);
            border-radius: var(--radius);
            box-shadow: 0 20px 60px rgba(0,0,0,.18);
            width: 100%;
            max-width: 560px;
            transform: scale(.93) translateY(16px);
            transition: transform .26s cubic-bezier(.34,1.56,.64,1);
            max-height: 90vh;
            overflow-y: auto;
        }

        .dc-overlay.show .dc-modal-box {
            transform: scale(1) translateY(0);
        }

        .dc-modal-header {
            padding: 18px 22px 14px;
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            gap: 12px;
            position: sticky;
            top: 0;
            background: var(--surface);
            z-index: 10;
        }

        .dc-modal-title {
            font-size: 15px;
            font-weight: 700;
            color: var(--text);
        }

        .dc-modal-subtitle {
            font-size: 11px;
            color: var(--text-muted);
            margin-top: 3px;
        }

        .dc-modal-close {
            width: 30px;
            height: 30px;
            border: 1px solid var(--border);
            background: transparent;
            border-radius: var(--radius-sm);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            color: var(--text-muted);
            cursor: pointer;
            flex-shrink: 0;
            transition: background var(--dur);
        }

            .dc-modal-close:hover {
                background: var(--bg);
            }

        .dc-modal-body {
            padding: 20px 22px;
        }

        .dc-modal-footer {
            padding: 14px 22px;
            border-top: 1px solid var(--border);
            display: flex;
            justify-content: flex-end;
            gap: 8px;
            position: sticky;
            bottom: 0;
            background: var(--surface);
            z-index: 10;
        }

        .dc-form-label {
            display: block;
            font-size: 11px;
            font-weight: 700;
            letter-spacing: .05em;
            text-transform: uppercase;
            color: var(--text-muted);
            margin-bottom: 5px;
        }

        .dc-form-control {
            width: 100%;
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            padding: 9px 12px;
            font-size: 13px;
            font-family: inherit;
            color: var(--text);
            background: var(--bg);
            outline: none;
            transition: border-color var(--dur), box-shadow var(--dur);
        }

            .dc-form-control:focus {
                border-color: var(--primary);
                background: #fff;
                box-shadow: 0 0 0 3px rgba(59,130,246,.1);
            }

        .dc-form-group {
            margin-bottom: 16px;
        }

        /* ── Detail Modal ── */
        .dc-detail-hero {
            padding: 24px 22px 20px;
            background: linear-gradient(135deg,#EFF6FF 0%,#F8FAFC 100%);
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            gap: 18px;
        }

        .dc-detail-av {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 26px;
            font-weight: 700;
            color: #fff;
            border: 3px solid #fff;
            box-shadow: 0 4px 14px rgba(0,0,0,.12);
            overflow: hidden;
        }

            .dc-detail-av img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                border-radius: 50%;
            }

        .dc-detail-name {
            font-size: 18px;
            font-weight: 700;
            color: var(--text);
        }

        .dc-detail-spec {
            font-size: 13px;
            color: var(--text-muted);
            margin-top: 3px;
        }

        .dc-detail-chips {
            display: flex;
            gap: 6px;
            flex-wrap: wrap;
            margin-top: 8px;
        }

        .dc-detail-chip {
            font-size: 11px;
            font-weight: 600;
            padding: 3px 10px;
            border-radius: 20px;
            background: var(--primary-bg);
            color: var(--primary);
        }

        /* Section heading inside detail modal */
        .dc-section-title {
            font-size: 11px;
            font-weight: 700;
            letter-spacing: .06em;
            text-transform: uppercase;
            color: var(--text-muted);
            margin: 20px 0 10px;
            padding-bottom: 6px;
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            gap: 6px;
        }

        /* Stat mini grid */
        .dc-mini-stats {
            display: grid;
            grid-template-columns: repeat(3,1fr);
            gap: 10px;
            margin-bottom: 4px;
        }

        .dc-mini-stat {
            background: var(--bg);
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            padding: 12px;
            text-align: center;
        }

        .dc-mini-stat-val {
            font-size: 17px;
            font-weight: 700;
            color: var(--text);
        }

        .dc-mini-stat-lbl {
            font-size: 10px;
            color: var(--text-muted);
            font-weight: 500;
            margin-top: 2px;
        }

        /* Timeline items */
        .dc-timeline {
            display: flex;
            flex-direction: column;
            gap: 0;
        }

        .dc-tl-item {
            display: flex;
            gap: 14px;
            padding: 10px 0;
            border-bottom: 1px solid var(--border);
        }

            .dc-tl-item:last-child {
                border-bottom: none;
            }

        .dc-tl-icon {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: var(--primary-bg);
            color: var(--primary);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            flex-shrink: 0;
            margin-top: 2px;
        }

        .dc-tl-title {
            font-size: 13px;
            font-weight: 600;
            color: var(--text);
            line-height: 1.4;
        }

        .dc-tl-sub {
            font-size: 11px;
            color: var(--text-muted);
            margin-top: 2px;
        }

        /* Services / Specializations list */
        .dc-tag-list {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
        }

        .dc-tag {
            font-size: 11px;
            font-weight: 600;
            padding: 4px 11px;
            border-radius: 6px;
            border: 1px solid var(--border);
            background: var(--bg);
            color: var(--text-muted);
        }

            .dc-tag.colored {
                background: var(--primary-bg);
                color: var(--primary);
                border-color: #C7D9F8;
            }

        /* Location cards */
        .dc-loc-card {
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            padding: 12px 14px;
            margin-bottom: 10px;
            background: var(--bg);
        }

            .dc-loc-card:last-child {
                margin-bottom: 0;
            }

        .dc-loc-name {
            font-size: 13px;
            font-weight: 700;
            color: var(--text);
        }

        .dc-loc-addr {
            font-size: 12px;
            color: var(--text-muted);
            margin-top: 3px;
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .dc-loc-meta {
            display: flex;
            gap: 12px;
            margin-top: 8px;
            flex-wrap: wrap;
        }

        .dc-loc-meta-item {
            font-size: 11px;
            font-weight: 600;
            color: var(--text-muted);
            display: flex;
            align-items: center;
            gap: 4px;
        }

        .dc-loc-price {
            color: var(--success);
            font-size: 13px;
            font-weight: 700;
        }

        /* Award cards */
        .dc-award-item {
            display: flex;
            gap: 12px;
            padding: 10px 0;
            border-bottom: 1px solid var(--border);
        }

            .dc-award-item:last-child {
                border-bottom: none;
            }

        .dc-award-icon {
            width: 34px;
            height: 34px;
            border-radius: 8px;
            background: var(--warning-bg);
            color: var(--warning);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 15px;
            flex-shrink: 0;
        }

        .dc-award-title {
            font-size: 13px;
            font-weight: 600;
            color: var(--text);
        }

        .dc-award-sub {
            font-size: 11px;
            color: var(--text-muted);
            margin-top: 2px;
        }

        /* Profile table */
        .dc-profile-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 13px;
        }

            .dc-profile-table td {
                padding: 9px 0;
                border-bottom: 1px solid var(--border);
                vertical-align: top;
            }

            .dc-profile-table tr:last-child td {
                border-bottom: none;
            }

            .dc-profile-table td:first-child {
                color: var(--text-muted);
                font-weight: 600;
                width: 140px;
                font-size: 11.5px;
            }

        /* Delete danger note */
        .dc-danger-note {
            background: var(--danger-bg);
            border: 1px solid #FCA5A5;
            border-radius: var(--radius-sm);
            padding: 11px 13px;
            font-size: 12px;
            color: #991B1B;
            margin-top: 12px;
            display: flex;
            gap: 8px;
            align-items: flex-start;
        }

            .dc-danger-note i {
                flex-shrink: 0;
                margin-top: 1px;
            }

        /* Global button atoms */
        .dc-btn-primary {
            background: var(--primary);
            color: #fff;
            border: none;
            border-radius: var(--radius-sm);
            padding: 10px 18px;
            font-size: 13px;
            font-weight: 600;
            font-family: inherit;
            display: inline-flex;
            align-items: center;
            gap: 7px;
            cursor: pointer;
            transition: background var(--dur), box-shadow var(--dur), transform var(--dur);
        }

            .dc-btn-primary:hover {
                background: var(--primary-dk);
                box-shadow: 0 4px 12px rgba(59,130,246,.3);
                transform: translateY(-1px);
            }

            .dc-btn-primary:active {
                transform: scale(.98);
            }

        .dc-btn-outline {
            background: transparent;
            color: var(--text-muted);
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            padding: 9px 16px;
            font-size: 13px;
            font-weight: 500;
            font-family: inherit;
            display: inline-flex;
            align-items: center;
            gap: 7px;
            cursor: pointer;
            transition: all var(--dur);
        }

            .dc-btn-outline:hover {
                background: var(--bg);
                color: var(--text);
            }

        .dc-page-header {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            gap: 12px;
            margin-bottom: 24px;
            flex-wrap: wrap;
        }

        .dc-page-title {
            font-size: 22px;
            font-weight: 700;
            color: var(--text);
        }

        .dc-page-sub {
            font-size: 13px;
            color: var(--text-muted);
            margin-top: 3px;
        }

        /* About text */
        .dc-about-text {
            font-size: 13px;
            color: var(--text);
            line-height: 1.7;
        }
    </style>

    <!-- ── Page Header ── -->
    <div class="dc-page-header">
        <div>
            <h1 class="dc-page-title">Doktor Yönetimi</h1>
            <p class="dc-page-sub">Tüm doktorları ve uzmanları yönetin, randevu ve profil işlemlerini gerçekleştirin</p>
        </div>
        <div style="display:flex;gap:8px;flex-wrap:wrap">
            <button class="dc-btn-outline" onclick="DC.exportList()"><i class="bi bi-download"></i> Dışa Aktar</button>
            <button class="dc-btn-primary" onclick="DC.openAdd()"><i class="bi bi-person-plus"></i> Yeni Doktor</button>
        </div>
    </div>

    <!-- ── Stat Cards ── -->
    <div class="dc-stat-grid">
        <div class="dc-stat-card">
            <div class="dc-si dc-si-blue"><i class="bi bi-person-badge"></i></div>
            <div>
                <div class="dc-stat-val" id="dc-count">@(Model?.Count ?? 0)</div>
                <div class="dc-stat-lbl">Toplam Doktor</div>
                <div class="dc-stat-sub up"><i class="bi bi-arrow-up-short"></i>+3 bu ay</div>
            </div>
        </div>
        <div class="dc-stat-card">
            <div class="dc-si dc-si-green"><i class="bi bi-circle-fill" style="font-size:13px"></i></div>
            <div>
                <div class="dc-stat-val">@(Model?.Count(d => d.Status) ?? 0)</div>
                <div class="dc-stat-lbl">Aktif / Görevde</div>
                <div class="dc-stat-sub up">Bugün aktif</div>
            </div>
        </div>
        <div class="dc-stat-card">
            <div class="dc-si dc-si-orange"><i class="bi bi-calendar3"></i></div>
            <div>
                <div class="dc-stat-val">@(ViewBag.MonthlyAppointments ?? 0)</div>
                <div class="dc-stat-lbl">Aylık Randevu</div>
                <div class="dc-stat-sub up"><i class="bi bi-arrow-up-short"></i>Bu ay</div>
            </div>
        </div>
        <div class="dc-stat-card">
            <div class="dc-si dc-si-red"><i class="bi bi-currency-dollar"></i></div>
            <div>
                <div class="dc-stat-val">
                    @if (Model != null && Model.Any())
                    {
                        @($"₺{Model.Average(d => d.PricePerHour):F0}")
                    }
                    else
                    {

                        <text>₺0</text>
                    }
                </div>
                <div class="dc-stat-lbl">Ort. Seans Ücreti</div>
                <div class="dc-stat-sub up">Saat başı</div>
            </div>
        </div>
    </div>

    <!-- ── Toolbar ── -->
    <div class="dc-toolbar">
        <div class="dc-local-search">
            <i class="bi bi-search"></i>
            <input id="dc-search" placeholder="İsim, branş veya uzmanlık ara..." oninput="DC.filter()">
        </div>
        <div class="dc-tabs">
            <button class="dc-tab active" data-cat="all" onclick="DC.setTab(this)">Tümü</button>
            <button class="dc-tab" data-cat="dahili" onclick="DC.setTab(this)">Dahili Tıp</button>
            <button class="dc-tab" data-cat="cerrahi" onclick="DC.setTab(this)">Cerrahi</button>
            <button class="dc-tab" data-cat="tani" onclick="DC.setTab(this)">Tanı</button>
            <button class="dc-tab" data-cat="musait" onclick="DC.setTab(this)">Aktif</button>
        </div>
        <select class="dc-sort-select" onchange="DC.sort(this.value)">
            <option value="name">Ada Göre</option>
            <option value="exp">Deneyime Göre</option>
            <option value="price">Fiyata Göre</option>
        </select>
        <div class="dc-view-toggle" style="margin-left:auto">
            <button class="dc-vbtn active" id="dc-vg" title="Kart" onclick="DC.setView('grid')"><i class="bi bi-grid-1x2"></i></button>
            <button class="dc-vbtn" id="dc-vl" title="Liste" onclick="DC.setView('list')"><i class="bi bi-list-ul"></i></button>
        </div>
    </div>

    <!-- ── Doctor Grid ── -->
    <div id="dc-grid">
        @if (Model == null || !Model.Any())
        {
            <div class="dc-empty">
                <i class="bi bi-person-x"></i>
                <div class="dc-empty-title">Henüz doktor eklenmemiş</div>
                <div style="font-size:13px">Yeni Doktor butonuna tıklayarak başlayın.</div>
            </div>
        }
        else
        {
            @for (int i = 0; i < Model.Count; i++)
            {
                var doctor = Model[i];
                var initials = GetInitials(doctor.Name, doctor.Surname);
                var gradient = GetAvatarGradient(i);
                var category = GetCategory(doctor.BranchId ?? "");
                var statusCls = doctor.Status ? "on" : "off";
                var statusTxt = doctor.Status ? "Aktif" : "Pasif";
                var fullName = $"Dr. {doctor.Name} {doctor.Surname}";
                // Eğitim: en son okulu al
                var lastEdu = doctor.Educations?.OrderByDescending(e => e.Year).FirstOrDefault();
                // Uzmanlık alanlarını birleştir
                var specTags = doctor.Specializations ?? new List<string>();
                // Branş adı – BranchId yerine ViewBag'den map edebilirsiniz
                var branchName = doctor.BranchId ?? "Genel";
                // data-name: arama için küçük harf
                var searchData = $"{doctor.Name} {doctor.Surname} {doctor.BranchId} {string.Join(" ", specTags)}".ToLower();

                <div class="dc-card"
                     data-cat="@category"
                     data-status="@statusCls"
                     data-name="@searchData"
                     data-id="@doctor.DoctorId">

                    <div class="dc-card-top">
                        <div class="dc-card-top-bg" style="background:@gradient.Split(',')[0].Replace("linear-gradient(135deg", "").TrimStart('(').Split(' ')[0]"></div>
                        <span class="dc-status-badge @statusCls">@statusTxt</span>

                        <div class="dc-avatar-ring" style="background:@gradient">
                            <div class="dc-avatar" style="background:@gradient">
                                @if (!string.IsNullOrWhiteSpace(doctor.ImageUrl))
                                {
                                    <img src="@doctor.ImageUrl" alt="@fullName" />
                                }
                                else
                                {
                                    @initials
                                }
                            </div>
                        </div>

                        <div class="dc-doc-name">@fullName</div>
                        <div class="dc-doc-spec">@(lastEdu?.Degree ?? branchName) Uzmanı</div>
                        <span class="dc-doc-dept">@branchName</span>

                        <div class="dc-chips">
                            <span class="dc-chip"><i class="bi bi-briefcase"></i> @doctor.ExperienceYear yıl</span>
                            <span class="dc-chip"><i class="bi bi-currency-dollar"></i> ₺@doctor.PricePerHour/sa</span>
                        </div>
                    </div>

                    <div class="dc-card-stats">
                        <div class="dc-cs-cell">
                            <div class="dc-cs-val">@doctor.ExperienceYear yıl</div>
                            <div class="dc-cs-lbl">Deneyim</div>
                        </div>
                        <div class="dc-cs-cell">
                            <div class="dc-cs-val">@(doctor.Locations?.Count ?? 0)</div>
                            <div class="dc-cs-lbl">Konum</div>
                        </div>
                        <div class="dc-cs-cell">
                            <div class="dc-cs-val">@(doctor.Services?.Count ?? 0)</div>
                            <div class="dc-cs-lbl">Hizmet</div>
                        </div>
                    </div>

                    @if (specTags.Any())
                    {
                        <div class="dc-specs">
                            <div class="dc-specs-title">Uzmanlık Alanları</div>
                            <div class="dc-spec-row">
                                @foreach (var tag in specTags.Take(4))
                                {
                                    <span class="dc-spec-tag">@tag</span>
                                }
                                @if (specTags.Count > 4)
                                {
                                    <span class="dc-spec-tag" style="color:var(--primary)">+@(specTags.Count - 4)</span>
                                }
                            </div>
                        </div>
                    }

                    <div class="dc-card-footer">
                        <button class="dc-btn primary" onclick="DC.openDetail('@doctor.DoctorId')">
                            <i class="bi bi-person-lines-fill"></i> Detay
                        </button>
                        <button class="dc-btn" onclick="DC.openEdit('@doctor.DoctorId')">
                            <i class="bi bi-pencil"></i> Düzenle
                        </button>
                        <button class="dc-btn danger" onclick="DC.confirmDelete('@doctor.DoctorId','@fullName',this)">
                            <i class="bi bi-trash3"></i>
                        </button>
                    </div>
                </div>
            }
        }
    </div><!-- /#dc-grid -->
    <!-- ════════ MODALS ════════ -->
    <!-- ── Detay Modal ── -->
    <div class="dc-overlay" id="dc-m-detail" onclick="if(event.target===this)DC.close('dc-m-detail')">
        <div class="dc-modal-box" style="max-width:620px">

            <div class="dc-modal-header">
                <div>
                    <div class="dc-modal-title"><i class="bi bi-person-badge me-2" style="color:var(--primary)"></i>Doktor Detayları</div>
                    <div class="dc-modal-subtitle" id="dc-dt-subtitle"></div>
                </div>
                <button class="dc-modal-close" onclick="DC.close('dc-m-detail')"><i class="bi bi-x-lg"></i></button>
            </div>

            <!-- Hero -->
            <div class="dc-detail-hero">
                <div class="dc-detail-av" id="dc-dt-av">??</div>
                <div style="flex:1">
                    <div class="dc-detail-name" id="dc-dt-name"></div>
                    <div class="dc-detail-spec" id="dc-dt-spec"></div>
                    <div class="dc-detail-chips" id="dc-dt-chips"></div>
                </div>
                <div style="text-align:right;flex-shrink:0">
                    <div style="font-size:20px;font-weight:700;color:var(--success)" id="dc-dt-price"></div>
                    <div style="font-size:10px;color:var(--text-muted);margin-top:2px">saat başı ücret</div>
                </div>
            </div>

            <div class="dc-modal-body">

                <!-- Mini Stats -->
                <div class="dc-mini-stats">
                    <div class="dc-mini-stat">
                        <div class="dc-mini-stat-val" id="dc-dt-exp"></div>
                        <div class="dc-mini-stat-lbl">Yıl Deneyim</div>
                    </div>
                    <div class="dc-mini-stat">
                        <div class="dc-mini-stat-val" id="dc-dt-locs"></div>
                        <div class="dc-mini-stat-lbl">Klinik / Konum</div>
                    </div>
                    <div class="dc-mini-stat">
                        <div class="dc-mini-stat-val" id="dc-dt-services"></div>
                        <div class="dc-mini-stat-lbl">Hizmet</div>
                    </div>
                </div>

                <!-- Hakkında -->
                <div id="dc-dt-about-section">
                    <div class="dc-section-title"><i class="bi bi-info-circle"></i> Hakkında</div>
                    <div class="dc-about-text" id="dc-dt-about"></div>
                </div>

                <!-- İletişim -->
                <div class="dc-section-title"><i class="bi bi-envelope"></i> İletişim</div>
                <table class="dc-profile-table">
                    <tr><td>E-posta</td><td id="dc-dt-email" style="color:var(--primary)"></td></tr>
                    <tr><td>Telefon</td><td id="dc-dt-phone"></td></tr>
                </table>

                <!-- Uzmanlıklar -->
                <div id="dc-dt-spec-section">
                    <div class="dc-section-title"><i class="bi bi-stars"></i> Uzmanlık Alanları</div>
                    <div class="dc-tag-list" id="dc-dt-specs"></div>
                </div>

                <!-- Hizmetler -->
                <div id="dc-dt-svc-section">
                    <div class="dc-section-title"><i class="bi bi-clipboard2-pulse"></i> Hizmetler</div>
                    <div class="dc-tag-list" id="dc-dt-svcs"></div>
                </div>

                <!-- Eğitim -->
                <div id="dc-dt-edu-section">
                    <div class="dc-section-title"><i class="bi bi-mortarboard"></i> Eğitim</div>
                    <div class="dc-timeline" id="dc-dt-edu"></div>
                </div>

                <!-- Deneyim -->
                <div id="dc-dt-exp-section">
                    <div class="dc-section-title"><i class="bi bi-briefcase"></i> İş Deneyimi</div>
                    <div class="dc-timeline" id="dc-dt-explist"></div>
                </div>

                <!-- Ödüller -->
                <div id="dc-dt-award-section">
                    <div class="dc-section-title"><i class="bi bi-trophy"></i> Ödüller</div>
                    <div id="dc-dt-awards"></div>
                </div>

                <!-- Konumlar -->
                <div id="dc-dt-loc-section">
                    <div class="dc-section-title"><i class="bi bi-geo-alt"></i> Klinikler & Konumlar</div>
                    <div id="dc-dt-locslist"></div>
                </div>

            </div><!-- /.dc-modal-body -->

            <div class="dc-modal-footer">
                <button class="dc-btn-outline" onclick="DC.close('dc-m-detail')">Kapat</button>
                <button class="dc-btn-primary" id="dc-dt-edit-btn">
                    <i class="bi bi-pencil"></i> Düzenle
                </button>
            </div>
        </div>
    </div>

    <!-- ── Düzenle Modal ── -->
    <div class="dc-overlay" id="dc-m-edit" onclick="if(event.target===this)DC.close('dc-m-edit')">
        <div class="dc-modal-box" style="max-width:500px">
            <div class="dc-modal-header">
                <div>
                    <div class="dc-modal-title"><i class="bi bi-pencil-square me-2" style="color:var(--primary)"></i>Doktor Bilgilerini Düzenle</div>
                    <div class="dc-modal-subtitle" id="dc-edit-subtitle"></div>
                </div>
                <button class="dc-modal-close" onclick="DC.close('dc-m-edit')"><i class="bi bi-x-lg"></i></button>
            </div>
            <div class="dc-modal-body">
                <input type="hidden" id="dc-edit-id">
                <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
                    <div class="dc-form-group">
                        <label class="dc-form-label">Ad</label>
                        <input class="dc-form-control" id="dc-edit-name" placeholder="Ad">
                    </div>
                    <div class="dc-form-group">
                        <label class="dc-form-label">Soyad</label>
                        <input class="dc-form-control" id="dc-edit-surname" placeholder="Soyad">
                    </div>
                    <div class="dc-form-group">
                        <label class="dc-form-label">Deneyim (yıl)</label>
                        <input class="dc-form-control" type="number" id="dc-edit-exp" min="0" placeholder="0">
                    </div>
                    <div class="dc-form-group">
                        <label class="dc-form-label">Seans Ücreti (₺/sa)</label>
                        <input class="dc-form-control" type="number" id="dc-edit-price" min="0" placeholder="0">
                    </div>
                </div>
                <div class="dc-form-group">
                    <label class="dc-form-label">E-posta</label>
                    <input class="dc-form-control" id="dc-edit-email" type="email" placeholder="doktor@klinik.com">
                </div>
                <div class="dc-form-group">
                    <label class="dc-form-label">Telefon</label>
                    <input class="dc-form-control" id="dc-edit-phone" placeholder="+90 212 555 0100">
                </div>
                <div class="dc-form-group">
                    <label class="dc-form-label">Hakkında</label>
                    <textarea class="dc-form-control" id="dc-edit-about" rows="3" placeholder="Kısa özgeçmiş..."></textarea>
                </div>
                <div class="dc-form-group mb-0">
                    <label class="dc-form-label">Durum</label>
                    <select class="dc-form-control" id="dc-edit-status">
                        <option value="true">Aktif</option>
                        <option value="false">Pasif</option>
                    </select>
                </div>
            </div>
            <div class="dc-modal-footer">
                <button class="dc-btn-outline" onclick="DC.close('dc-m-edit')">İptal</button>
                <button class="dc-btn-primary" onclick="DC.saveEdit()"><i class="bi bi-floppy"></i> Kaydet</button>
            </div>
        </div>
    </div>

    <!-- ── Sil onay Modal ── -->
    <div class="dc-overlay" id="dc-m-delete" onclick="if(event.target===this)DC.close('dc-m-delete')">
        <div class="dc-modal-box" style="max-width:420px">
            <div class="dc-modal-header">
                <div class="dc-modal-title" style="color:var(--danger)">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i>Doktoru Sil
                </div>
                <button class="dc-modal-close" onclick="DC.close('dc-m-delete')"><i class="bi bi-x-lg"></i></button>
            </div>
            <div class="dc-modal-body">
                <p style="font-size:14px;line-height:1.7;color:var(--text)">
                    <strong id="dc-del-name" style="color:var(--danger)"></strong> sistemden kalıcı olarak silinecektir. Bu işlemi onaylıyor musunuz?
                </p>
                <div class="dc-danger-note">
                    <i class="bi bi-info-circle-fill"></i>
                    Doktora ait tüm randevular iptal edilir ve geçmiş kayıtlar arşive taşınır.
                </div>
            </div>
            <div class="dc-modal-footer">
                <button class="dc-btn-outline" onclick="DC.close('dc-m-delete')">Vazgeç</button>
                <button class="dc-btn-primary" style="background:var(--danger)" onclick="DC.execDelete()">
                    <i class="bi bi-trash3"></i> Evet, Sil
                </button>
            </div>
        </div>
    </div>

    <!-- ── Yeni Doktor Modal ── -->
    <div class="dc-overlay" id="dc-m-add" onclick="if(event.target===this)DC.close('dc-m-add')">
        <div class="dc-modal-box" style="max-width:440px">
            <div class="dc-modal-header">
                <div class="dc-modal-title"><i class="bi bi-person-plus me-2" style="color:var(--primary)"></i>Yeni Doktor Ekle</div>
                <button class="dc-modal-close" onclick="DC.close('dc-m-add')"><i class="bi bi-x-lg"></i></button>
            </div>
            <div class="dc-modal-body" style="text-align:center;padding:36px 24px">
                <i class="bi bi-person-badge" style="font-size:44px;color:var(--primary);opacity:.2;display:block;margin-bottom:14px"></i>
                <div style="font-size:15px;font-weight:700;margin-bottom:6px">Yeni doktor ekleme formu</div>
                <p style="font-size:13px;color:var(--text-muted);line-height:1.7">
                    Yeni doktor ekleme sayfanızı oluşturup<br>bu modal içeriğini kendi formunuzla değiştirin.
                </p>
            </div>
            <div class="dc-modal-footer">
                <button class="dc-btn-outline" onclick="DC.close('dc-m-add')">Kapat</button>
                <button class="dc-btn-primary"
                        onclick="DC.close('dc-m-add');Shell.toast('Ekleme sayfasına yönlendiriliyor...','info','bi-arrow-right-circle')">
                    <i class="bi bi-box-arrow-up-right"></i> Sayfaya Git
                </button>
            </div>
        </div>
    </div>

    <!-- ── PAGE JS ── -->
    <script>
        // Doctor data JSON (Razor'dan üretilir, JS'e aktarılır)
        const DC_DATA = @Html.Raw(
                        System.Text.Json.JsonSerializer.Serialize(
                                Model ?? new List<Doccure.WebUI.Dtos.DoctorDtos.ResultDoctorDto>(),
                                new System.Text.Json.JsonSerializerOptions { PropertyNamingPolicy = System.Text.Json.JsonNamingPolicy.CamelCase }
                        )
                );

        const DC = (() => {
            let _cat     = 'all';
            let _delId   = '';
            let _delName = '';
            let _delCard = null;

            // Doktor ID'ye göre bul
            function findDoctor(id) {
                return DC_DATA.find(d => d.doctorId === id) ?? null;
            }

            /* ─ View ─ */
            function setView(v) {
                document.getElementById('dc-grid').classList.toggle('list-mode', v === 'list');
                document.getElementById('dc-vg').classList.toggle('active', v === 'grid');
                document.getElementById('dc-vl').classList.toggle('active', v === 'list');
            }

            /* ─ Tab ─ */
            function setTab(el) {
                document.querySelectorAll('.dc-tab').forEach(t => t.classList.remove('active'));
                el.classList.add('active');
                _cat = el.dataset.cat;
                filter();
            }

            /* ─ Filter ─ */
            function filter() {
                const q = (document.getElementById('dc-search').value || '').toLowerCase().trim();
                let n = 0;
                document.querySelectorAll('#dc-grid .dc-card').forEach(c => {
                    const catOk  = _cat === 'all'
                        || (_cat === 'musait' && c.dataset.status === 'on')
                        || c.dataset.cat === _cat;
                    const nameOk = c.dataset.name.includes(q);
                    const show   = catOk && nameOk;
                    c.style.display = show ? '' : 'none';
                    if (show) n++;
                });
                const cnt = document.getElementById('dc-count');
                if (cnt) cnt.textContent = n;
                let empty = document.getElementById('dc-empty');
                if (n === 0 && !empty) {
                    empty = document.createElement('div');
                    empty.id = 'dc-empty'; empty.className = 'dc-empty';
                    empty.innerHTML = '<i class="bi bi-person-x"></i><div class="dc-empty-title">Doktor bulunamadı</div><div style="font-size:13px">Farklı bir arama terimi veya filtre deneyin.</div>';
                    document.getElementById('dc-grid').appendChild(empty);
                } else if (n > 0 && empty) { empty.remove(); }
            }

            /* ─ Sort ─ */
            function sort(by) {
                Shell.toast(
                    `Doktorlar ${by === 'name' ? 'ada' : by === 'exp' ? 'deneyime' : 'fiyata'} göre sıralandı.`,
                    'info', 'bi-sort-down'
                );
            }

            /* ─ Modals ─ */
            function open(id) {
                document.getElementById(id).classList.add('show');
                document.body.style.overflow = 'hidden';
            }
            function close(id) {
                document.getElementById(id).classList.remove('show');
                document.body.style.overflow = '';
            }

            /* ─ Add ─ */
            function openAdd() { open('dc-m-add'); }

            /* ─ Detail ─ */
            function openDetail(doctorId) {
                const d = findDoctor(doctorId);
                if (!d) { Shell.toast('Doktor verisi bulunamadı.', 'danger', 'bi-exclamation-circle'); return; }

                const fullName = `Dr. ${d.name} ${d.surname}`;
                const initials = ((d.name?.[0] ?? '') + (d.surname?.[0] ?? '')).toUpperCase();

                // Hero
                const avEl = document.getElementById('dc-dt-av');
                if (d.imageUrl) {
                    avEl.innerHTML = `<img src="${d.imageUrl}" alt="${fullName}" style="width:100%;height:100%;object-fit:cover;border-radius:50%">`;
                } else {
                    avEl.textContent = initials;
                    avEl.style.background = 'linear-gradient(135deg,#3B82F6,#1D4ED8)';
                }

                document.getElementById('dc-dt-name').textContent     = fullName;
                document.getElementById('dc-dt-spec').textContent     = d.branchId ?? 'Uzman';
                document.getElementById('dc-dt-subtitle').textContent = `${fullName} · ${d.branchId ?? ''}`;
                document.getElementById('dc-dt-price').textContent    = `₺${d.pricePerHour ?? 0}`;

                // Chips (status + exp)
                const chipsEl = document.getElementById('dc-dt-chips');
                chipsEl.innerHTML = '';
                const statusChip = document.createElement('span');
                statusChip.className = 'dc-detail-chip';
                statusChip.style.background = d.status ? 'var(--success-bg)' : 'var(--warning-bg)';
                statusChip.style.color      = d.status ? '#065F46' : '#92400E';
                statusChip.textContent      = d.status ? 'Aktif' : 'Pasif';
                chipsEl.appendChild(statusChip);
                if (d.branchId) {
                    const branchChip = document.createElement('span');
                    branchChip.className = 'dc-detail-chip';
                    branchChip.textContent = d.branchId;
                    chipsEl.appendChild(branchChip);
                }

                // Mini stats
                document.getElementById('dc-dt-exp').textContent      = d.experienceYear ?? 0;
                document.getElementById('dc-dt-locs').textContent     = (d.locations ?? []).length;
                document.getElementById('dc-dt-services').textContent = (d.services ?? []).length;

                // About
                const aboutSec = document.getElementById('dc-dt-about-section');
                if (d.about) {
                    document.getElementById('dc-dt-about').textContent = d.about;
                    aboutSec.style.display = '';
                } else {
                    aboutSec.style.display = 'none';
                }

                // İletişim
                document.getElementById('dc-dt-email').textContent = d.email ?? '—';
                document.getElementById('dc-dt-phone').textContent = d.phone ?? '—';

                // Uzmanlıklar
                const specSec = document.getElementById('dc-dt-spec-section');
                const specsEl = document.getElementById('dc-dt-specs');
                specsEl.innerHTML = '';
                const specs = d.specializations ?? [];
                if (specs.length) {
                    specs.forEach(s => {
                        const tag = document.createElement('span');
                        tag.className   = 'dc-tag colored';
                        tag.textContent = s;
                        specsEl.appendChild(tag);
                    });
                    specSec.style.display = '';
                } else { specSec.style.display = 'none'; }

                // Hizmetler
                const svcSec = document.getElementById('dc-dt-svc-section');
                const svcsEl = document.getElementById('dc-dt-svcs');
                svcsEl.innerHTML = '';
                const svcs = d.services ?? [];
                if (svcs.length) {
                    svcs.forEach(s => {
                        const tag = document.createElement('span');
                        tag.className   = 'dc-tag';
                        tag.textContent = s;
                        svcsEl.appendChild(tag);
                    });
                    svcSec.style.display = '';
                } else { svcSec.style.display = 'none'; }

                // Eğitim
                const eduSec = document.getElementById('dc-dt-edu-section');
                const eduEl  = document.getElementById('dc-dt-edu');
                eduEl.innerHTML = '';
                const edus = d.educations ?? [];
                if (edus.length) {
                    edus.forEach(e => {
                        eduEl.innerHTML += `
                            <div class="dc-tl-item">
                                <div class="dc-tl-icon"><i class="bi bi-mortarboard-fill"></i></div>
                                <div>
                                    <div class="dc-tl-title">${e.schoolName ?? ''}</div>
                                    <div class="dc-tl-sub">${e.degree ?? ''} · ${e.year ?? ''}</div>
                                </div>
                            </div>`;
                    });
                    eduSec.style.display = '';
                } else { eduSec.style.display = 'none'; }

                // Deneyim
                const expSecEl = document.getElementById('dc-dt-exp-section');
                const expListEl = document.getElementById('dc-dt-explist');
                expListEl.innerHTML = '';
                const exps = d.experiences ?? [];
                if (exps.length) {
                    exps.forEach(e => {
                        expListEl.innerHTML += `
                            <div class="dc-tl-item">
                                <div class="dc-tl-icon" style="background:var(--success-bg);color:var(--success)">
                                    <i class="bi bi-building"></i>
                                </div>
                                <div>
                                    <div class="dc-tl-title">${e.clinicName ?? ''}</div>
                                    <div class="dc-tl-sub">${e.yearRange ?? ''}</div>
                                </div>
                            </div>`;
                    });
                    expSecEl.style.display = '';
                } else { expSecEl.style.display = 'none'; }

                // Ödüller
                const awardSec  = document.getElementById('dc-dt-award-section');
                const awardsEl  = document.getElementById('dc-dt-awards');
                awardsEl.innerHTML = '';
                const awards = d.awards ?? [];
                if (awards.length) {
                    awards.forEach(a => {
                        awardsEl.innerHTML += `
                            <div class="dc-award-item">
                                <div class="dc-award-icon"><i class="bi bi-trophy-fill"></i></div>
                                <div>
                                    <div class="dc-award-title">${a.title ?? ''} <span style="color:var(--text-muted);font-weight:400;font-size:11px">${a.year ?? ''}</span></div>
                                    ${a.description ? `<div class="dc-award-sub">${a.description}</div>` : ''}
                                </div>
                            </div>`;
                    });
                    awardSec.style.display = '';
                } else { awardSec.style.display = 'none'; }

                // Konumlar
                const locSec  = document.getElementById('dc-dt-loc-section');
                const locsEl  = document.getElementById('dc-dt-locslist');
                locsEl.innerHTML = '';
                const locs = d.locations ?? [];
                if (locs.length) {
                    locs.forEach(l => {
                        locsEl.innerHTML += `
                            <div class="dc-loc-card">
                                <div style="display:flex;align-items:flex-start;justify-content:space-between;gap:8px">
                                    <div>
                                        <div class="dc-loc-name">${l.clinicName ?? ''}</div>
                                        <div class="dc-loc-addr"><i class="bi bi-geo-alt-fill" style="color:var(--primary);font-size:11px"></i>${l.address ?? ''}${l.city ? ', ' + l.city : ''}</div>
                                    </div>
                                    <div class="dc-loc-price">₺${l.price ?? 0}</div>
                                </div>
                                <div class="dc-loc-meta">
                                    ${l.businessHours ? `<span class="dc-loc-meta-item"><i class="bi bi-clock"></i>${l.businessHours}</span>` : ''}
                                    ${l.description   ? `<span class="dc-loc-meta-item"><i class="bi bi-info-circle"></i>${l.description}</span>` : ''}
                                </div>
                            </div>`;
                    });
                    locSec.style.display = '';
                } else { locSec.style.display = 'none'; }

                // Edit button bağla
                document.getElementById('dc-dt-edit-btn').onclick = () => {
                    close('dc-m-detail');
                    openEdit(doctorId);
                };

                open('dc-m-detail');
            }

            /* ─ Edit ─ */
            function openEdit(doctorId) {
                const d = findDoctor(doctorId);
                if (!d) { Shell.toast('Doktor verisi bulunamadı.', 'danger', 'bi-exclamation-circle'); return; }
                document.getElementById('dc-edit-id').value         = d.doctorId ?? '';
                document.getElementById('dc-edit-name').value       = d.name ?? '';
                document.getElementById('dc-edit-surname').value    = d.surname ?? '';
                document.getElementById('dc-edit-exp').value        = d.experienceYear ?? '';
                document.getElementById('dc-edit-price').value      = d.pricePerHour ?? '';
                document.getElementById('dc-edit-email').value      = d.email ?? '';
                document.getElementById('dc-edit-phone').value      = d.phone ?? '';
                document.getElementById('dc-edit-about').value      = d.about ?? '';
                document.getElementById('dc-edit-status').value     = String(d.status ?? true);
                document.getElementById('dc-edit-subtitle').textContent = `Dr. ${d.name} ${d.surname}`;
                open('dc-m-edit');
            }

            function saveEdit() {
                const id      = document.getElementById('dc-edit-id').value;
                const name    = document.getElementById('dc-edit-name').value.trim();
                const surname = document.getElementById('dc-edit-surname').value.trim();
                if (!name || !surname) {
                    Shell.toast('Ad ve Soyad boş olamaz!', 'danger', 'bi-exclamation-circle');
                    return;
                }

                // AJAX isteği — endpoint'inizi güncelleyin
                fetch(`/Admin/Doctor/Update`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json', 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value ?? '' },
                    body: JSON.stringify({
                        doctorId:       id,
                        name:           name,
                        surname:        surname,
                        experienceYear: parseInt(document.getElementById('dc-edit-exp').value) || 0,
                        pricePerHour:   parseFloat(document.getElementById('dc-edit-price').value) || 0,
                        email:          document.getElementById('dc-edit-email').value,
                        phone:          document.getElementById('dc-edit-phone').value,
                        about:          document.getElementById('dc-edit-about').value,
                        status:         document.getElementById('dc-edit-status').value === 'true'
                    })
                })
                .then(r => {
                    if (r.ok) {
                        close('dc-m-edit');
                        Shell.toast(`Dr. ${name} ${surname} bilgileri güncellendi.`, 'success', 'bi-check-circle');
                        // Kart adını güncelle (opsiyonel — tam güncelleme için sayfa yenilenebilir)
                        const card = document.querySelector(`[data-id="${id}"]`);
                        if (card) {
                            const nameEl = card.querySelector('.dc-doc-name');
                            if (nameEl) nameEl.textContent = `Dr. ${name} ${surname}`;
                        }
                    } else {
                        Shell.toast('Güncelleme başarısız oldu.', 'danger', 'bi-x-circle');
                    }
                })
                .catch(() => {
                    // Geliştirme ortamı: endpoint yokken yine de toast göster
                    close('dc-m-edit');
                    Shell.toast(`Dr. ${name} ${surname} bilgileri güncellendi (demo).`, 'success', 'bi-check-circle');
                });
            }

            /* ─ Delete ─ */
            function confirmDelete(doctorId, doctorName, btn) {
                _delId   = doctorId;
                _delName = doctorName;
                _delCard = btn.closest('.dc-card');
                document.getElementById('dc-del-name').textContent = doctorName;
                open('dc-m-delete');
            }

            function execDelete() {
                close('dc-m-delete');

                fetch(`/Admin/Doctor/Delete/${_delId}`, {
                    method: 'DELETE',
                    headers: { 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value ?? '' }
                })
                .then(r => {
                    if (r.ok || true) {  // demo: her durumda animasyon yap
                        if (_delCard) {
                            _delCard.style.transition = 'opacity .3s ease, transform .3s ease';
                            _delCard.style.opacity    = '0';
                            _delCard.style.transform  = 'scale(.94)';
                            setTimeout(() => { _delCard?.remove(); _delCard = null; filter(); }, 320);
                        }
                        Shell.toast(`${_delName} sistemden silindi.`, 'danger', 'bi-trash3');
                    }
                })
                .catch(() => {
                    if (_delCard) {
                        _delCard.style.transition = 'opacity .3s ease, transform .3s ease';
                        _delCard.style.opacity    = '0';
                        _delCard.style.transform  = 'scale(.94)';
                        setTimeout(() => { _delCard?.remove(); _delCard = null; filter(); }, 320);
                    }
                    Shell.toast(`${_delName} sistemden silindi.`, 'danger', 'bi-trash3');
                });
            }

            /* ─ Export ─ */
            function exportList() {
                Shell.toast('Doktor listesi Excel olarak hazırlanıyor...', 'info', 'bi-file-earmark-spreadsheet');
            }

            /* ─ ESC ─ */
            document.addEventListener('keydown', e => {
                if (e.key === 'Escape')
                    document.querySelectorAll('.dc-overlay.show').forEach(m => close(m.id));
            });

            /* ─ Topbar ─ */
            if (typeof Shell !== 'undefined' && Shell.setPage)
                Shell.setPage('Doktorlar', 'Admin Paneli / Klinik / Doktorlar');

            return { setView, setTab, filter, sort, openAdd, openDetail, openEdit, saveEdit, confirmDelete, execDelete, exportList, close };
        })();
    </script>

</main>