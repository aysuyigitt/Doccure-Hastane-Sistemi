@model List<ResultBranchDto>
@{
    ViewData["Title"] = "BranchList";
    Layout = "~/Areas/Admin/Views/AdminLayout/Index.cshtml";

    // Toplam istatistikler
    int totalBranches = Model?.Count ?? 0;
    int totalDoctors = Model?.Sum(b => b.DoctorCount) ?? 0;
    int totalAppointments = Model?.Sum(b => b.MonthlyAppointmentCount) ?? 0;
    decimal avgRating = Model != null && Model.Any() ? Math.Round(Model.Average(b => b.Rating), 1) : 0;


    string GetCategory(ResultBranchDto b)
    {

        if (string.IsNullOrEmpty(b.ThemeColor)) return "dahili";
        string tc = b.ThemeColor.ToLower();
        if (tc.Contains("f59e0b") || tc.Contains("fcd34d") || tc.Contains("f97316")) return "cerrahi";
        if (tc.Contains("6366f1") || tc.Contains("a5b4fc") || tc.Contains("0ea5e9") || tc.Contains("06b6d4")) return "tani";
        return "dahili";
    }

    string GetOccupancyColor(int rate)
    {
        if (rate >= 80) return "var(--primary)";
        if (rate >= 60) return "var(--warning)";
        return "var(--success)";
    }

    string GetStatusClass(bool status) => status ? "on" : "off";
    string GetStatusLabel(bool status) => status ? "Aktif" : "Pasif";
}

<main id="app-content">

    <style>
        /* ── Stat Grid ── */
        .bc-stat-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 14px;
            margin-bottom: 24px;
        }

        @@media(max-width:900px) {
            .bc-stat-grid {
                grid-template-columns: repeat(2,1fr);
            }
        }

        @@media(max-width:480px) {
            .bc-stat-grid {
                grid-template-columns: 1fr 1fr;
                gap: 10px;
            }
        }

        .bc-stat-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 18px 20px;
            display: flex;
            align-items: center;
            gap: 14px;
            box-shadow: var(--shadow);
            transition: box-shadow var(--dur) var(--ease), transform var(--dur) var(--ease);
        }

            .bc-stat-card:hover {
                box-shadow: 0 4px 20px rgba(59,130,246,.13);
                transform: translateY(-2px);
            }

        .bc-stat-icon {
            width: 44px;
            height: 44px;
            border-radius: 12px;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 19px;
        }

        .bc-si-blue {
            background: var(--primary-bg);
            color: var(--primary);
        }

        .bc-si-green {
            background: var(--success-bg);
            color: var(--success);
        }

        .bc-si-orange {
            background: var(--warning-bg);
            color: var(--warning);
        }

        .bc-si-purple {
            background: #F3F0FF;
            color: #7C3AED;
        }

        .bc-stat-val {
            font-size: 22px;
            font-weight: 700;
            color: var(--text);
            line-height: 1.1;
        }

        .bc-stat-lbl {
            font-size: 11px;
            color: var(--text-muted);
            font-weight: 500;
            margin-top: 2px;
        }

        /* ── Toolbar ── */
        .bc-toolbar {
            display: flex;
            align-items: center;
            gap: 10px;
            flex-wrap: wrap;
            margin-bottom: 20px;
        }

        .bc-local-search {
            position: relative;
            flex: 1;
            min-width: 160px;
            max-width: 280px;
        }

            .bc-local-search i {
                position: absolute;
                left: 11px;
                top: 50%;
                transform: translateY(-50%);
                color: var(--text-muted);
                font-size: 13px;
                pointer-events: none;
            }

            .bc-local-search input {
                width: 100%;
                border: 1px solid var(--border);
                border-radius: 20px;
                padding: 8px 14px 8px 32px;
                font-size: 13px;
                font-family: inherit;
                background: var(--surface);
                color: var(--text);
                outline: none;
                transition: border-color var(--dur), box-shadow var(--dur);
            }

                .bc-local-search input:focus {
                    border-color: var(--primary);
                    box-shadow: 0 0 0 3px rgba(59,130,246,.1);
                }

                .bc-local-search input::placeholder {
                    color: var(--text-muted);
                }

        .bc-tabs {
            display: flex;
            gap: 5px;
            flex-wrap: wrap;
        }

        .bc-tab {
            border: 1px solid var(--border);
            background: var(--surface);
            border-radius: 20px;
            padding: 7px 15px;
            font-size: 12px;
            font-weight: 600;
            color: var(--text-muted);
            cursor: pointer;
            transition: all var(--dur) var(--ease);
            white-space: nowrap;
        }

            .bc-tab:hover {
                background: var(--primary-bg);
                color: var(--primary);
                border-color: #C7D9F8;
            }

            .bc-tab.active {
                background: var(--primary);
                color: #fff;
                border-color: var(--primary);
            }

        .bc-view-toggle {
            display: flex;
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            overflow: hidden;
            flex-shrink: 0;
        }

        .bc-vbtn {
            width: 34px;
            height: 34px;
            border: none;
            background: var(--surface);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            color: var(--text-muted);
            cursor: pointer;
            transition: all var(--dur);
        }

            .bc-vbtn.active {
                background: var(--primary-bg);
                color: var(--primary);
            }

        /* ── Card Grid ── */
        #bc-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 18px;
        }

            #bc-grid.list-mode {
                grid-template-columns: 1fr;
            }

        /* Entry animation */
        .bc-card {
            animation: bcFadeUp .3s var(--ease) both;
        }

            .bc-card:nth-child(1) {
                animation-delay: .04s
            }

            .bc-card:nth-child(2) {
                animation-delay: .08s
            }

            .bc-card:nth-child(3) {
                animation-delay: .12s
            }

            .bc-card:nth-child(4) {
                animation-delay: .16s
            }

            .bc-card:nth-child(5) {
                animation-delay: .20s
            }

            .bc-card:nth-child(6) {
                animation-delay: .24s
            }

        @@keyframes bcFadeUp {
            from {
                opacity: 0;
                transform: translateY(16px)
            }

            to {
                opacity: 1;
                transform: translateY(0)
            }
        }

        .bc-card {
            background: var(--surface);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            display: flex;
            flex-direction: column;
            overflow: hidden;
            transition: box-shadow var(--dur) var(--ease), transform var(--dur) var(--ease), border-color var(--dur);
        }

            .bc-card:hover {
                box-shadow: 0 4px 24px rgba(59,130,246,.13);
                transform: translateY(-3px);
                border-color: #C7D9F8;
            }

        .bc-card-accent {
            height: 4px;
            width: 100%;
            flex-shrink: 0;
        }

        .bc-card-body {
            padding: 18px 20px 14px;
            flex: 1;
        }

        .bc-card-head {
            display: flex;
            align-items: flex-start;
            gap: 13px;
            margin-bottom: 12px;
        }

        .bc-card-icon {
            width: 50px;
            height: 50px;
            border-radius: 13px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            flex-shrink: 0;
            overflow: hidden;
        }

            .bc-card-icon img {
                width: 100%;
                height: 100%;
                object-fit: cover;
                border-radius: 13px;
            }

        .bc-card-info {
            flex: 1;
            min-width: 0;
        }

        .bc-card-name {
            font-size: 15px;
            font-weight: 700;
            color: var(--text);
            margin-bottom: 4px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .bc-card-code {
            font-family: monospace;
            font-size: 10px;
            font-weight: 600;
            background: var(--bg);
            color: var(--text-muted);
            padding: 2px 7px;
            border-radius: 4px;
            display: inline-block;
            margin-right: 5px;
        }

        .bc-status-dot {
            display: inline-block;
            width: 7px;
            height: 7px;
            border-radius: 50%;
            vertical-align: middle;
            margin-right: 4px;
        }

            .bc-status-dot.on {
                background: var(--success);
                box-shadow: 0 0 0 3px rgba(16,185,129,.18);
            }

            .bc-status-dot.off {
                background: #94A3B8;
            }

        .bc-status-lbl {
            font-size: 11px;
            color: var(--text-muted);
            vertical-align: middle;
        }

        .bc-card-desc {
            font-size: 13px;
            color: var(--text-muted);
            line-height: 1.65;
            margin-bottom: 14px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        /* Mini stats */
        .bc-mini-stats {
            display: flex;
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            overflow: hidden;
            margin-bottom: 13px;
        }

        .bc-ms-cell {
            flex: 1;
            text-align: center;
            padding: 9px 4px;
            border-right: 1px solid var(--border);
            background: var(--bg);
            transition: background var(--dur);
        }

            .bc-ms-cell:last-child {
                border-right: none;
            }

            .bc-ms-cell:hover {
                background: var(--primary-bg);
            }

        .bc-ms-val {
            font-size: 15px;
            font-weight: 700;
            color: var(--text);
            line-height: 1.1;
        }

        .bc-ms-lbl {
            font-size: 10px;
            color: var(--text-muted);
            font-weight: 500;
            margin-top: 2px;
        }

        /* Occupancy */
        .bc-occ-row {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 13px;
        }

        .bc-occ-lbl {
            font-size: 11px;
            color: var(--text-muted);
            white-space: nowrap;
        }

        .bc-prog-track {
            flex: 1;
            height: 5px;
            background: var(--border);
            border-radius: 10px;
            overflow: hidden;
        }

        .bc-prog-fill {
            height: 100%;
            border-radius: 10px;
            transition: width .6s var(--ease);
        }

        .bc-occ-pct {
            font-size: 11px;
            font-weight: 700;
            white-space: nowrap;
            min-width: 30px;
            text-align: right;
        }

        /* Doctor avatars */
        .bc-docs-row {
            display: flex;
            align-items: center;
        }

        .bc-av {
            width: 26px;
            height: 26px;
            border-radius: 50%;
            border: 2px solid var(--surface);
            color: #fff;
            font-size: 9px;
            font-weight: 700;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: -9px;
            flex-shrink: 0;
        }

        .bc-av-more {
            width: 26px;
            height: 26px;
            border-radius: 50%;
            border: 2px solid var(--surface);
            background: var(--bg);
            color: var(--text-muted);
            font-size: 9px;
            font-weight: 700;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: -9px;
        }

        .bc-docs-lbl {
            font-size: 11px;
            color: var(--text-muted);
            margin-left: 18px;
        }

        .bc-rating {
            display: flex;
            align-items: center;
            gap: 3px;
            margin-left: auto;
            font-size: 12px;
            font-weight: 700;
            color: var(--text);
        }

            .bc-rating i {
                color: #FBBF24;
                font-size: 11px;
            }

        /* Expand panel */
        .bc-expand {
            display: none;
            border-top: 1px solid var(--border);
            background: #F8FAFC;
            padding: 14px 20px;
        }

            .bc-expand.open {
                display: block;
                animation: bcFadeUp .2s var(--ease);
            }

        .bc-expand-title {
            font-size: 10.5px;
            font-weight: 700;
            letter-spacing: .07em;
            text-transform: uppercase;
            color: var(--text-muted);
            margin-bottom: 10px;
        }

        .bc-doc-row {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 9px 0;
            border-bottom: 1px dashed var(--border);
        }

            .bc-doc-row:last-child {
                border-bottom: none;
                padding-bottom: 0;
            }

        .bc-doc-av {
            width: 34px;
            height: 34px;
            border-radius: 50%;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 11px;
            font-weight: 700;
            color: #fff;
        }

        .bc-doc-name {
            font-size: 13px;
            font-weight: 600;
            color: var(--text);
        }

        .bc-doc-spec {
            font-size: 11px;
            color: var(--text-muted);
        }

        .bc-doc-badge {
            font-size: 10px;
            font-weight: 600;
            padding: 3px 9px;
            border-radius: 20px;
            white-space: nowrap;
            flex-shrink: 0;
        }

            .bc-doc-badge.on {
                background: var(--success-bg);
                color: #065F46;
            }

            .bc-doc-badge.off {
                background: var(--warning-bg);
                color: #92400E;
            }

        /* Card footer */
        .bc-card-footer {
            padding: 11px 14px;
            border-top: 1px solid var(--border);
            background: #FAFBFE;
            display: flex;
            gap: 6px;
        }

        .bc-btn {
            flex: 1;
            border: 1px solid var(--border);
            background: var(--surface);
            border-radius: var(--radius-sm);
            padding: 7px 8px;
            font-size: 12px;
            font-weight: 600;
            font-family: inherit;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 5px;
            cursor: pointer;
            transition: all var(--dur) var(--ease);
            color: var(--text-muted);
            white-space: nowrap;
        }

            .bc-btn.primary {
                color: var(--primary);
                border-color: #C7D9F8;
                background: var(--primary-bg);
            }

                .bc-btn.primary:hover {
                    background: var(--primary);
                    color: #fff;
                    border-color: var(--primary);
                    box-shadow: 0 3px 10px rgba(59,130,246,.25);
                }

            .bc-btn.danger:hover {
                background: var(--danger-bg);
                border-color: #FCA5A5;
                color: var(--danger);
            }

            .bc-btn:hover {
                background: var(--bg);
                color: var(--text);
            }

        /* List mode */
        #bc-grid.list-mode .bc-card {
            flex-direction: row;
        }

        #bc-grid.list-mode .bc-card-accent {
            width: 4px;
            height: auto;
        }

        #bc-grid.list-mode .bc-card-body {
            display: flex;
            align-items: center;
            gap: 16px;
            flex-wrap: wrap;
            padding: 12px 18px;
        }

        #bc-grid.list-mode .bc-card-head {
            margin-bottom: 0;
            flex-shrink: 0;
        }

        #bc-grid.list-mode .bc-card-info {
            min-width: 130px;
        }

        #bc-grid.list-mode .bc-card-desc {
            display: none;
        }

        #bc-grid.list-mode .bc-mini-stats {
            margin-bottom: 0;
            flex-shrink: 0;
            min-width: 196px;
        }

        #bc-grid.list-mode .bc-occ-row {
            display: none;
        }

        #bc-grid.list-mode .bc-docs-row {
            flex-shrink: 0;
        }

        #bc-grid.list-mode .bc-card-footer {
            flex-direction: column;
            border-top: none;
            border-left: 1px solid var(--border);
            padding: 11px 11px;
            min-width: 116px;
            gap: 5px;
            justify-content: center;
        }

        #bc-grid.list-mode .bc-btn {
            flex: unset;
            width: 100%;
        }

        #bc-grid.list-mode .bc-expand {
            display: none !important;
        }

        /* Empty state */
        .bc-empty {
            grid-column: 1/-1;
            text-align: center;
            padding: 72px 24px;
            color: var(--text-muted);
        }

            .bc-empty i {
                font-size: 48px;
                opacity: .15;
                display: block;
                margin-bottom: 14px;
            }

        .bc-empty-title {
            font-size: 15px;
            font-weight: 700;
            color: var(--text);
            margin-bottom: 6px;
        }

        /* Modal */
        .bc-modal-danger-note {
            background: var(--danger-bg);
            border: 1px solid #FCA5A5;
            border-radius: var(--radius-sm);
            padding: 11px 13px;
            font-size: 12px;
            color: #991B1B;
            margin-top: 12px;
            display: flex;
            gap: 8px;
            align-items: flex-start;
        }

            .bc-modal-danger-note i {
                flex-shrink: 0;
                margin-top: 1px;
            }

        .bc-page-header {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            gap: 12px;
            margin-bottom: 24px;
            flex-wrap: wrap;
        }

        .bc-page-title {
            font-size: 22px;
            font-weight: 700;
            color: var(--text);
        }

        .bc-page-sub {
            font-size: 13px;
            color: var(--text-muted);
            margin-top: 3px;
        }

        .bc-btn-primary {
            background: var(--primary);
            color: #fff;
            border: none;
            border-radius: var(--radius-sm);
            padding: 10px 18px;
            font-size: 13px;
            font-weight: 600;
            font-family: inherit;
            display: inline-flex;
            align-items: center;
            gap: 7px;
            cursor: pointer;
            transition: background var(--dur), box-shadow var(--dur), transform var(--dur);
        }

            .bc-btn-primary:hover {
                background: var(--primary-dk);
                box-shadow: 0 4px 12px rgba(59,130,246,.3);
                transform: translateY(-1px);
            }

            .bc-btn-primary:active {
                transform: scale(.98);
            }

        .bc-btn-outline {
            background: transparent;
            color: var(--text-muted);
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            padding: 9px 16px;
            font-size: 13px;
            font-weight: 500;
            font-family: inherit;
            display: inline-flex;
            align-items: center;
            gap: 7px;
            cursor: pointer;
            transition: all var(--dur);
        }

            .bc-btn-outline:hover {
                background: var(--bg);
                color: var(--text);
            }

        .bc-overlay {
            position: fixed;
            inset: 0;
            background: rgba(15,23,42,.45);
            z-index: 500;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 16px;
            opacity: 0;
            pointer-events: none;
            transition: opacity .2s ease;
        }

            .bc-overlay.show {
                opacity: 1;
                pointer-events: all;
            }

        .bc-modal-box {
            background: var(--surface);
            border-radius: var(--radius);
            box-shadow: 0 20px 60px rgba(0,0,0,.18);
            width: 100%;
            max-width: 500px;
            transform: scale(.93) translateY(16px);
            transition: transform .26s cubic-bezier(.34,1.56,.64,1);
        }

        .bc-overlay.show .bc-modal-box {
            transform: scale(1) translateY(0);
        }

        .bc-modal-header {
            padding: 18px 22px 14px;
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            gap: 12px;
        }

        .bc-modal-title {
            font-size: 15px;
            font-weight: 700;
            color: var(--text);
        }

        .bc-modal-subtitle {
            font-size: 11px;
            color: var(--text-muted);
            margin-top: 3px;
        }

        .bc-modal-close {
            width: 30px;
            height: 30px;
            border: 1px solid var(--border);
            background: transparent;
            border-radius: var(--radius-sm);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            color: var(--text-muted);
            cursor: pointer;
            flex-shrink: 0;
            transition: background var(--dur);
        }

            .bc-modal-close:hover {
                background: var(--bg);
            }

        .bc-modal-body {
            padding: 20px 22px;
        }

        .bc-modal-footer {
            padding: 14px 22px;
            border-top: 1px solid var(--border);
            display: flex;
            justify-content: flex-end;
            gap: 8px;
        }

        .bc-form-label {
            display: block;
            font-size: 11px;
            font-weight: 700;
            letter-spacing: .05em;
            text-transform: uppercase;
            color: var(--text-muted);
            margin-bottom: 5px;
        }

        .bc-form-control {
            width: 100%;
            border: 1px solid var(--border);
            border-radius: var(--radius-sm);
            padding: 9px 12px;
            font-size: 13px;
            font-family: inherit;
            color: var(--text);
            background: var(--bg);
            outline: none;
            transition: border-color var(--dur), box-shadow var(--dur);
        }

            .bc-form-control:focus {
                border-color: var(--primary);
                background: #fff;
                box-shadow: 0 0 0 3px rgba(59,130,246,.1);
            }

        .bc-form-group {
            margin-bottom: 16px;
        }

        /* Stat grid */
        .bc-stat-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 14px;
            margin-bottom: 24px;
        }
    </style>

    <!-- ── Page Header ── -->
    <div class="bc-page-header">
        <div>
            <h1 class="bc-page-title">Branş Yönetimi</h1>
            <p class="bc-page-sub">Tüm tıbbi branşları yönetin, doktorları görüntüleyin ve randevu alın</p>
        </div>
        <div style="display:flex;gap:8px;flex-wrap:wrap">
            <button class="bc-btn-outline" onclick="BC.exportList()">
                <i class="bi bi-download"></i> Dışa Aktar
            </button>
            <a class="bc-btn-primary" href="/Admin/Branch/CreateBranch">
                <i class="bi bi-plus-lg"></i> Yeni Branş
            </a>
        </div>
    </div>

    <!-- ── Stat Cards ── -->
    <div class="bc-stat-grid">
        <div class="bc-stat-card">
            <div class="bc-stat-icon bc-si-blue"><i class="bi bi-diagram-3"></i></div>
            <div>
                <div class="bc-stat-val" id="bc-count">@totalBranches</div>
                <div class="bc-stat-lbl">Toplam Branş</div>
            </div>
        </div>
        <div class="bc-stat-card">
            <div class="bc-stat-icon bc-si-green"><i class="bi bi-person-badge"></i></div>
            <div>
                <div class="bc-stat-val">@totalDoctors</div>
                <div class="bc-stat-lbl">Aktif Doktor</div>
            </div>
        </div>
        <div class="bc-stat-card">
            <div class="bc-stat-icon bc-si-orange"><i class="bi bi-calendar3"></i></div>
            <div>
                <div class="bc-stat-val">@totalAppointments</div>
                <div class="bc-stat-lbl">Aylık Randevu</div>
            </div>
        </div>
        <div class="bc-stat-card">
            <div class="bc-stat-icon bc-si-purple"><i class="bi bi-star-fill"></i></div>
            <div>
                <div class="bc-stat-val">@avgRating</div>
                <div class="bc-stat-lbl">Ort. Memnuniyet</div>
            </div>
        </div>
    </div>

    <!-- ── Toolbar ── -->
    <div class="bc-toolbar">
        <div class="bc-local-search">
            <i class="bi bi-search"></i>
            <input id="bc-search" placeholder="Branş ara..." oninput="BC.filter()">
        </div>
        <div class="bc-tabs">
            <button class="bc-tab active" data-cat="all" onclick="BC.setTab(this)">Tümü</button>
            <button class="bc-tab" data-cat="dahili" onclick="BC.setTab(this)">Dahili Tıp</button>
            <button class="bc-tab" data-cat="cerrahi" onclick="BC.setTab(this)">Cerrahi</button>
            <button class="bc-tab" data-cat="tani" onclick="BC.setTab(this)">Tanı</button>
        </div>
        <div class="bc-view-toggle" style="margin-left:auto">
            <button class="bc-vbtn active" id="bc-vg" title="Kart" onclick="BC.setView('grid')"><i class="bi bi-grid-1x2"></i></button>
            <button class="bc-vbtn" id="bc-vl" title="Liste" onclick="BC.setView('list')"><i class="bi bi-list-ul"></i></button>
        </div>
    </div>

    <!-- ── Branch Grid ── -->
    <div id="bc-grid">

        @if (Model == null || !Model.Any())
        {
            <div class="bc-empty">
                <i class="bi bi-diagram-3"></i>
                <div class="bc-empty-title">Branş bulunamadı</div>
                <div style="font-size:13px">Henüz kayıtlı branş yok. Yeni branş ekleyin.</div>
            </div>
        }
        else
        {
            @for (int i = 0; i < Model.Count; i++)
            {
                var b = Model[i];
                var cat = GetCategory(b);
                var dataName = b.BranchName?.ToLower(new System.Globalization.CultureInfo("tr-TR")) ?? "";
                var occColor = GetOccupancyColor(b.OccupancyRate);
                var statusClass = GetStatusClass(b.Status);
                var statusLabel = GetStatusLabel(b.Status);

                // Accent gradient: ThemeColor varsa onu kullan, yoksa varsayılan mavi
                var accentColor = string.IsNullOrEmpty(b.ThemeColor)
                ? "linear-gradient(90deg,#3B82F6,#60A5FA)"
                : b.ThemeColor;

                // İkon arka planı (ThemeColor'dan hafif ton)
                var iconBg = "#EFF6FF";

                // Serizalizasyon için JSON-safe string'ler
                var jName = System.Text.Json.JsonSerializer.Serialize(b.BranchName ?? "");
                var jDesc = System.Text.Json.JsonSerializer.Serialize(b.Description ?? "");
                var jCode = System.Text.Json.JsonSerializer.Serialize(b.BranchCode ?? "");
                var jId = System.Text.Json.JsonSerializer.Serialize(b.BranchId ?? "");
                var jCat = System.Text.Json.JsonSerializer.Serialize(cat);

                <div class="bc-card" data-cat="@cat" data-name="@dataName" data-id="@b.BranchId">

                    <!-- Accent bar -->
                    <div class="bc-card-accent" style="background:@accentColor"></div>

                    <div class="bc-card-body">
                        <div class="bc-card-head">

                            <!-- İkon / Görsel -->
                            <div class="bc-card-icon" style="background:@iconBg">
                                @if (!string.IsNullOrEmpty(b.ImageUrl))
                                {
                                    @b.ImageUrl
                                }
                                else
                                {
                                    <i class="bi bi-hospital" style="font-size:22px;color:#3B82F6"></i>
                                }
                            </div>



                            <div class="bc-card-info">
                                <div class="bc-card-name" title="@b.BranchName">@b.BranchName</div>
                                <div>
                                    <span class="bc-card-code">@b.BranchCode</span>
                                    <span class="bc-status-dot @statusClass"></span>
                                    <span class="bc-status-lbl">@statusLabel</span>
                                </div>
                            </div>
                        </div>

                        <!-- Açıklama -->
                        <p class="bc-card-desc">@b.Description</p>

                        <!-- Mini istatistikler -->
                        <div class="bc-mini-stats">
                            <div class="bc-ms-cell">
                                <div class="bc-ms-val">@b.DoctorCount</div>
                                <div class="bc-ms-lbl">Doktor</div>
                            </div>
                            <div class="bc-ms-cell">
                                <div class="bc-ms-val">@b.MonthlyAppointmentCount</div>
                                <div class="bc-ms-lbl">Randevu/ay</div>
                            </div>
                            <div class="bc-ms-cell">
                                <div class="bc-ms-val">@b.BedCount</div>
                                <div class="bc-ms-lbl">Yatak</div>
                            </div>
                            <div class="bc-ms-cell">
                                <div class="bc-ms-val">@b.Rating.ToString("F1")</div>
                                <div class="bc-ms-lbl">Puan</div>
                            </div>
                        </div>

                        <!-- Doluluk oranı -->
                        <div class="bc-occ-row">
                            <span class="bc-occ-lbl">Doluluk</span>
                            <div class="bc-prog-track">
                                <div class="bc-prog-fill"
                                     style="width:@(b.OccupancyRate)%;background:@occColor"></div>
                            </div>
                            <span class="bc-occ-pct" style="color:@occColor">@(b.OccupancyRate)%</span>
                        </div>

                        <!-- Doktor avatarları (sayıya göre dinamik) -->
                        <div class="bc-docs-row">
                            @{
                                // İlk 3 avatar için placeholder initials (gerçek doktor listesi ayrı endpoint'ten gelebilir)
                                int avatarCount = Math.Min(b.DoctorCount, 3);
                                int remaining = b.DoctorCount - avatarCount;
                                string[] avatarColors = { "#3B82F6", "#60A5FA", "#93C5FD", "#7C3AED", "#10B981", "#F59E0B" };
                                string avatarColor = avatarColors[i % avatarColors.Length];
                            }

                            @for (int a = 0; a < avatarCount; a++)
                            {
                                <div class="bc-av" style="background:@avatarColors[(i + a) % avatarColors.Length]">
                                    <i class="bi bi-person-fill" style="font-size:11px"></i>
                                </div>
                            }

                            @if (remaining > 0)
                            {
                                <div class="bc-av-more">+@remaining</div>
                            }

                            <span class="bc-docs-lbl">@b.DoctorCount doktor</span>

                            <div class="bc-rating">
                                <i class="bi bi-star-fill"></i>@b.Rating.ToString("F1")
                            </div>
                        </div>
                    </div>

                    <!-- Doktor listesi expand paneli -->
                    <!-- Not: Doktor detayları ayrı bir API/endpoint'ten yüklenecekse
                         BC.loadDoctors(branchId) metodunu kullanın. -->
                    <div class="bc-expand" id="bc-exp-@i">
                        <div class="bc-expand-title">Doktorlar</div>
                        <div class="bc-doc-empty" style="text-align:center;padding:16px 0;color:var(--text-muted);font-size:13px">
                            <i class="bi bi-arrow-repeat" style="font-size:20px;display:block;margin-bottom:6px"></i>
                            Doktor listesi yükleniyor...
                        </div>
                    </div>

                    <!-- Card footer -->
                    <div class="bc-card-footer">
                        <button class="bc-btn primary"
                                onclick="BC.toggleDocs(@i, '@b.BranchId', this)">
                            <i class="bi bi-people"></i> Doktorlar
                        </button>
                        <a href="/admin/Branch/UpdateBranch/@b.BranchId" class="bc-btn">
                            <i class="bi bi-pencil"></i> Düzenle
                        </a>
                        <button class="bc-btn danger"
                                onclick="BC.confirmDelete(@jName, @jId, this)">
                            <i class="bi bi-trash3"></i> Sil
                        </button>
                    </div>
                </div>
            }
        }

    </div><!-- /#bc-grid -->
    <!-- ── MODALLER ── -->
    <!-- Düzenle -->
    <div class="bc-overlay" id="bc-m-edit" onclick="if(event.target===this)BC.close('bc-m-edit')">
        <div class="bc-modal-box" style="max-width:520px">
            <div class="bc-modal-header">
                <div>
                    <div class="bc-modal-title">
                        <i class="bi bi-pencil-square me-2" style="color:var(--primary)"></i>Branşı Düzenle
                    </div>
                    <div class="bc-modal-subtitle" id="bc-edit-code"></div>
                </div>
                <button class="bc-modal-close" onclick="BC.close('bc-m-edit')">
                    <i class="bi bi-x-lg"></i>
                </button>
            </div>
            <div class="bc-modal-body">
                <input type="hidden" id="bc-edit-id">
                <div class="bc-form-group">
                    <label class="bc-form-label">Branş Adı</label>
                    <input class="bc-form-control" id="bc-edit-name" placeholder="Örn: Kardiyoloji">
                </div>
                <div class="bc-form-group">
                    <label class="bc-form-label">Açıklama</label>
                    <textarea class="bc-form-control" id="bc-edit-desc" rows="3"></textarea>
                </div>
                <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:10px">
                    <div>
                        <label class="bc-form-label">Kategori</label>
                        <select class="bc-form-control" id="bc-edit-cat">
                            <option value="dahili">Dahili Tıp</option>
                            <option value="cerrahi">Cerrahi</option>
                            <option value="tani">Tanı</option>
                        </select>
                    </div>
                    <div>
                        <label class="bc-form-label">Doktor Sayısı</label>
                        <input class="bc-form-control" type="number" id="bc-edit-docs" min="0">
                    </div>
                    <div>
                        <label class="bc-form-label">Durum</label>
                        <select class="bc-form-control" id="bc-edit-status">
                            <option value="true">Aktif</option>
                            <option value="false">Pasif</option>
                        </select>
                    </div>
                </div>
            </div>
            <div class="bc-modal-footer">
                <button class="bc-btn-outline" onclick="BC.close('bc-m-edit')">İptal</button>
                <button class="bc-btn-primary" onclick="BC.saveEdit()">
                    <i class="bi bi-floppy"></i> Kaydet
                </button>
            </div>
        </div>
    </div>

    <!-- Sil onay -->
    <div class="bc-overlay" id="bc-m-delete" onclick="if(event.target===this)BC.close('bc-m-delete')">
        <div class="bc-modal-box" style="max-width:420px">
            <div class="bc-modal-header">
                <div class="bc-modal-title" style="color:var(--danger)">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i>Branşı Sil
                </div>
                <button class="bc-modal-close" onclick="BC.close('bc-m-delete')">
                    <i class="bi bi-x-lg"></i>
                </button>
            </div>
            <div class="bc-modal-body">
                <p style="font-size:14px;line-height:1.7;color:var(--text)">
                    <strong id="bc-del-name" style="color:var(--danger)"></strong>
                    branşını kalıcı olarak silmek istediğinizden emin misiniz?
                </p>
                <div class="bc-modal-danger-note">
                    <i class="bi bi-info-circle-fill"></i>
                    Bu işlem geri alınamaz. Branşa ait randevu ve kayıtlar arşive taşınır.
                </div>
            </div>
            <div class="bc-modal-footer">
                <button class="bc-btn-outline" onclick="BC.close('bc-m-delete')">Vazgeç</button>
                <button class="bc-btn-primary" style="background:var(--danger)" onclick="BC.execDelete()">
                    <i class="bi bi-trash3"></i> Evet, Sil
                </button>
            </div>
        </div>
    </div>

    <!-- Yeni Branş -->
    <div class="bc-overlay" id="bc-m-add" onclick="if(event.target===this)BC.close('bc-m-add')">
        <div class="bc-modal-box" style="max-width:440px">
            <div class="bc-modal-header">
                <div class="bc-modal-title">
                    <i class="bi bi-plus-circle me-2" style="color:var(--primary)"></i>Yeni Branş Ekle
                </div>
                <button class="bc-modal-close" onclick="BC.close('bc-m-add')">
                    <i class="bi bi-x-lg"></i>
                </button>
            </div>
            <div class="bc-modal-body" style="text-align:center;padding:36px 24px">
                <i class="bi bi-layout-text-window-reverse"
                   style="font-size:44px;color:var(--primary);opacity:.2;display:block;margin-bottom:14px"></i>
                <div style="font-size:15px;font-weight:700;margin-bottom:6px">Sayfa henüz bağlanmadı</div>
                <p style="font-size:13px;color:var(--text-muted);line-height:1.7">
                    Yeni branş ekleme sayfanızı oluşturup<br>bu modal içeriğini kendi formunuzla değiştirin.
                </p>
            </div>
            <div class="bc-modal-footer">
                <button class="bc-btn-outline" onclick="BC.close('bc-m-add')">Kapat</button>
                <button class="bc-btn-primary"
                        onclick="BC.close('bc-m-add');Shell.toast('Ekleme sayfasına yönlendiriliyor...','info','bi-arrow-right-circle')">
                    <i class="bi bi-box-arrow-up-right"></i> Sayfaya Git
                </button>
            </div>
        </div>
    </div>

    <!-- ── PAGE JS ── -->
    <script>
        const BC = (() => {
            let _cat     = 'all';
            let _delName = '';
            let _delId   = '';
            let _delCard = null;

            /* ─ View ─ */
            function setView(v) {
                document.getElementById('bc-grid').classList.toggle('list-mode', v === 'list');
                document.getElementById('bc-vg').classList.toggle('active', v === 'grid');
                document.getElementById('bc-vl').classList.toggle('active', v === 'list');
                if (v === 'list') _closeAllExpand();
            }

            /* ─ Tab ─ */
            function setTab(el) {
                document.querySelectorAll('.bc-tab').forEach(t => t.classList.remove('active'));
                el.classList.add('active');
                _cat = el.dataset.cat;
                filter();
            }

            /* ─ Filter ─ */
            function filter() {
                const q = (document.getElementById('bc-search').value || '').toLowerCase().trim();
                let n = 0;
                document.querySelectorAll('#bc-grid .bc-card').forEach(c => {
                    const show = (_cat === 'all' || c.dataset.cat === _cat)
                              && (c.dataset.name || '').includes(q);
                    c.style.display = show ? '' : 'none';
                    if (show) n++;
                });

                // Stat sayacını güncelle
                const cnt = document.getElementById('bc-count');
                if (cnt) cnt.textContent = n;

                // Empty state
                let empty = document.getElementById('bc-empty-dyn');
                if (n === 0 && !empty) {
                    empty = document.createElement('div');
                    empty.id = 'bc-empty-dyn';
                    empty.className = 'bc-empty';
                    empty.innerHTML = '<i class="bi bi-search"></i>'
                        + '<div class="bc-empty-title">Sonuç bulunamadı</div>'
                        + '<div style="font-size:13px">Farklı arama terimi veya kategori deneyin.</div>';
                    document.getElementById('bc-grid').appendChild(empty);
                } else if (n > 0 && empty) {
                    empty.remove();
                }
            }

            /* ─ Docs expand ─ */
            function toggleDocs(i, branchId, btn) {
                const panel = document.getElementById('bc-exp-' + i);
                const open  = panel.classList.contains('open');
                _closeAllExpand();
                if (!open) {
                    panel.classList.add('open');
                    btn.innerHTML = '<i class="bi bi-chevron-up"></i> Gizle';
                    // Doktor listesi zaten yüklenmediyse fetch et
                    if (!panel.dataset.loaded) {
                        _loadDoctors(panel, branchId);
                    }
                }
            }

            function _loadDoctors(panel, branchId) {
                // Gerçek endpoint'inize göre URL'yi güncelleyin
                // Örnek: /Admin/Branch/GetDoctors?branchId=...
                fetch(`/Admin/Branch/GetDoctors?branchId=${branchId}`)
                    .then(r => {
                        if (!r.ok) throw new Error('HTTP ' + r.status);
                        return r.json();
                    })
                    .then(doctors => {
                        panel.dataset.loaded = 'true';
                        if (!doctors || doctors.length === 0) {
                            panel.innerHTML = '<div class="bc-expand-title">Doktorlar</div>'
                                + '<div style="text-align:center;padding:16px 0;color:var(--text-muted);font-size:13px">'
                                + 'Bu branşa kayıtlı doktor bulunamadı.</div>';
                            return;
                        }

                        let html = '<div class="bc-expand-title">Doktorlar</div>';
                        const colors = ['#3B82F6','#10B981','#F59E0B','#7C3AED','#06B6D4','#6366F1'];
                        doctors.forEach((d, idx) => {
                            const initials = (d.fullName || 'Dr')
                                .split(' ').filter(Boolean)
                                .slice(0, 2).map(w => w[0].toUpperCase()).join('');
                            const color    = colors[idx % colors.length];
                            const avail    = d.isAvailable !== false;
                            const badgeCls = avail ? 'on' : 'off';
                            const badgeTxt = avail ? 'Müsait' : 'Meşgul';
                            const btnHtml  = avail
                                ? `<button class="bc-appt-btn" onclick="BC.book('${d.fullName}')"><i class="bi bi-calendar-plus"></i> Randevu</button>`
                                : `<button class="bc-appt-btn" disabled><i class="bi bi-clock"></i> Dolu</button>`;
                            html += `<div class="bc-doc-row">
                                <div class="bc-doc-av" style="background:${color}">${initials}</div>
                                <div>
                                    <div class="bc-doc-name">${d.fullName || ''}</div>
                                    <div class="bc-doc-spec">${d.specialty || ''}</div>
                                </div>
                                <span class="bc-doc-badge ${badgeCls}">${badgeTxt}</span>
                                ${btnHtml}
                            </div>`;
                        });

                        panel.innerHTML = html;
                    })
                    .catch(() => {
                        panel.innerHTML = '<div class="bc-expand-title">Doktorlar</div>'
                            + '<div style="text-align:center;padding:16px 0;color:var(--danger);font-size:13px">'
                            + '<i class="bi bi-exclamation-circle" style="display:block;margin-bottom:6px"></i>'
                            + 'Doktorlar yüklenemedi.</div>';
                    });
            }

            function _closeAllExpand() {
                document.querySelectorAll('.bc-expand.open').forEach(p => p.classList.remove('open'));
                document.querySelectorAll('.bc-btn.primary').forEach(b => {
                    if (b.textContent.includes('Gizle')) {
                        b.innerHTML = '<i class="bi bi-people"></i> Doktorlar';
                    }
                });
            }

            /* ─ Appointment ─ */
            function book(doc) {
                Shell.toast(`${doc} için randevu talebi oluşturuldu!`, 'success', 'bi-calendar-check');
            }

            /* ─ Modal helpers ─ */
            function open(id) {
                document.getElementById(id).classList.add('show');
                document.body.style.overflow = 'hidden';
            }
            function close(id) {
                document.getElementById(id).classList.remove('show');
                document.body.style.overflow = '';
            }

            /* ─ Add ─ */
            function openAdd() { open('bc-m-add'); }

            /* ─ Edit ─ */
            function openEdit(d) {
                document.getElementById('bc-edit-id').value   = d.id   || '';
                document.getElementById('bc-edit-name').value = d.name || '';
                document.getElementById('bc-edit-desc').value = d.desc || '';
                document.getElementById('bc-edit-docs').value = d.docs || '';
                document.getElementById('bc-edit-code').textContent = 'Kod: ' + (d.code || '');
                document.getElementById('bc-edit-cat').value  = d.cat  || 'dahili';
                open('bc-m-edit');
            }

            function saveEdit() {
                const n  = document.getElementById('bc-edit-name').value.trim();
                const id = document.getElementById('bc-edit-id').value;
                if (!n) {
                    Shell.toast('Branş adı boş olamaz!', 'danger', 'bi-exclamation-circle');
                    return;
                }

                // Gerçek API çağrısı için:
                // fetch('/Admin/Branch/Update', { method:'POST', headers:{'Content-Type':'application/json'},
                //   body: JSON.stringify({ BranchId: id, BranchName: n, ... }) })
                // .then(r => r.json()).then(res => { if(res.success) { close('bc-m-edit'); location.reload(); } });

                close('bc-m-edit');
                Shell.toast(`"${n}" branşı güncellendi.`, 'success', 'bi-check-circle');
            }

                  /* ─ Delete ─ */
        function confirmDelete(name, id, btn) {
            _delName = name;
            _delId   = id;
            _delCard = btn.closest('.bc-card');
            document.getElementById('bc-del-name').textContent = name;
            open('bc-m-delete');
        }

                function execDelete() {
            close('bc-m-delete');

            fetch(`/admin/Branch/deletebranch/${_delId}`, {
                method: 'POST',
                redirect: 'manual'
            })
            .then(r => {
                if (r.ok || r.type === 'opaqueredirect') {
                    _removeCard();
                    Shell.toast(`"${_delName}" branşı silindi.`, 'danger', 'bi-trash3');
                } else {
                    Shell.toast('Silme işlemi başarısız.', 'warning', 'bi-exclamation-circle');
                }
            })
            .catch(err => {
                Shell.toast('Bir hata oluştu.', 'danger', 'bi-x-circle');
            });
        }

        function _removeCard() {
            if (_delCard) {
                _delCard.style.transition = 'opacity .3s ease, transform .3s ease';
                _delCard.style.opacity    = '0';
                _delCard.style.transform  = 'scale(.94)';
                setTimeout(() => { _delCard?.remove(); _delCard = null; filter(); }, 320);
            }
        }

            /* ─ Export ─ */
            function exportList() {
                // Gerçek export endpoint'i:
                // window.location.href = '/Admin/Branch/Export';
                Shell.toast('Branş listesi Excel olarak hazırlanıyor...', 'info', 'bi-file-earmark-spreadsheet');
            }

            /* ─ ESC ─ */
            document.addEventListener('keydown', e => {
                if (e.key === 'Escape')
                    document.querySelectorAll('.bc-overlay.show').forEach(m => close(m.id));
            });

            /* ─ Topbar başlığını güncelle ─ */
            if (typeof Shell !== 'undefined' && Shell.setPage)
                Shell.setPage('Branşlar', 'Admin Paneli / Klinik / Branşlar');

            return {
                setView, setTab, filter, toggleDocs,
                book, openAdd, openEdit, saveEdit,
                confirmDelete, execDelete, exportList, close
            };
        })();
    </script>

</main>