@{
    Layout = null;
}


<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 — Sayfa Bulunamadı | HastaneSis</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&family=Sora:wght@700;800&display=swap" rel="stylesheet" />
    <style>
        *, *::before, *::after {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        :root {
            --cyan: #06b6c8;
            --cyan-deep: #0891b2;
            --cyan-pale: #e0f7fa;
            --blue: #2563a8;
            --blue-soft: #eff6ff;
            --teal: #0d9488;
            --mint: #d1fae5;
            --purple-soft: #ede9fe;
            --purple: #7c3aed;
            --white: #ffffff;
            --gray-400: #94a3b8;
            --gray-500: #64748b;
            --text-dark: #0b1f3a;
            --text-mid: #334155;
            --glass: rgba(255,255,255,0.70);
            --glass-border: rgba(255,255,255,0.90);
            --shadow: 0 24px 70px rgba(6,100,130,0.14), 0 4px 20px rgba(6,100,130,0.07);
        }

        html, body {
            min-height: 100vh;
            font-family: 'Plus Jakarta Sans', sans-serif;
            overflow-x: hidden;
        }

        body {
            background: linear-gradient(150deg, #bfe8f6 0%, #cdf0f9 35%, #d6eef8 65%, #c8dff2 100%);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 70px 1rem 58px;
            position: relative;
        }

        /* ── Background layer ── */
        .bg-layer {
            position: fixed;
            inset: 0;
            z-index: 0;
            overflow: hidden;
        }

            .bg-layer svg {
                position: absolute;
                width: 100%;
                height: 100%;
            }

        /* ── Orbs ── */
        .orb {
            position: fixed;
            border-radius: 50%;
            pointer-events: none;
            z-index: 1;
        }

        .orb-1 {
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, rgba(6,182,200,0.16) 0%, transparent 70%);
            top: -160px;
            left: -140px;
            animation: drift 17s ease-in-out infinite alternate;
        }

        .orb-2 {
            width: 420px;
            height: 420px;
            background: radial-gradient(circle, rgba(37,99,168,0.13) 0%, transparent 70%);
            bottom: -130px;
            right: -110px;
            animation: drift 22s ease-in-out infinite alternate-reverse;
        }

        .orb-3 {
            width: 260px;
            height: 260px;
            background: radial-gradient(circle, rgba(124,58,237,0.08) 0%, transparent 70%);
            top: 35%;
            right: 6%;
            animation: drift 26s ease-in-out infinite alternate;
        }

        @@keyframes drift {
            0% {
                transform: translate(0,0) scale(1);
            }

            50% {
                transform: translate(35px,-45px) scale(1.07);
            }

            100% {
                transform: translate(-20px,25px) scale(0.95);
            }
        }

        /* ── Floating medical items ── */
        .float-items {
            position: fixed;
            inset: 0;
            z-index: 1;
            pointer-events: none;
        }

        .fi {
            position: absolute;
            opacity: 0;
            animation: floatUp linear infinite;
        }

        @@keyframes floatUp {
            0% {
                transform: translateY(0) rotate(0deg);
                opacity: 0;
            }

            10% {
                opacity: 1;
            }

            90% {
                opacity: 0.7;
            }

            100% {
                transform: translateY(-110vh) rotate(360deg);
                opacity: 0;
            }
        }

        /* ── Topbar ── */
        .topbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            height: 54px;
            background: rgba(255,255,255,0.82);
            backdrop-filter: blur(18px);
            -webkit-backdrop-filter: blur(18px);
            border-bottom: 1px solid rgba(6,182,200,0.14);
            display: flex;
            align-items: center;
            padding: 0 1.8rem;
            z-index: 200;
            box-shadow: 0 2px 12px rgba(6,100,130,0.06);
        }

        .tb-logo {
            display: flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
        }

        .tb-icon {
            width: 32px;
            height: 32px;
            background: linear-gradient(135deg, var(--cyan), var(--blue));
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .tb-wordmark {
            font-weight: 700;
            font-size: 14.5px;
            color: var(--text-dark);
            letter-spacing: -0.3px;
        }

            .tb-wordmark em {
                color: var(--cyan);
                font-style: normal;
            }

        .tb-chip {
            margin-left: 10px;
            background: var(--cyan-pale);
            color: var(--cyan-deep);
            font-size: 10px;
            font-weight: 700;
            letter-spacing: 0.7px;
            text-transform: uppercase;
            padding: 2px 8px;
            border-radius: 20px;
        }

        .tb-right {
            margin-left: auto;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .tb-pill {
            display: flex;
            align-items: center;
            gap: 5px;
            background: rgba(124,58,237,0.09);
            border: 1px solid rgba(124,58,237,0.20);
            color: #5b21b6;
            font-size: 11.5px;
            font-weight: 600;
            padding: 3px 10px;
            border-radius: 20px;
        }

        .tb-pill-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: var(--purple);
            animation: blink 1.8s ease-in-out infinite;
        }

        @@keyframes blink {
            0%, 100% {
                opacity: 1;
            }

            50% {
                opacity: 0.3;
            }
        }

        /* ── Main card ── */
        .card-wrap {
            position: relative;
            z-index: 10;
            width: 100%;
            max-width: 600px;
            background: var(--glass);
            backdrop-filter: blur(32px);
            -webkit-backdrop-filter: blur(32px);
            border: 1.5px solid var(--glass-border);
            border-radius: 28px;
            box-shadow: var(--shadow);
            padding: 3rem 2.8rem 2.6rem;
            animation: riseIn 0.78s cubic-bezier(0.34,1.1,0.64,1) both;
        }

        @@keyframes riseIn {
            from {
                opacity: 0;
                transform: translateY(42px) scale(0.95);
            }

            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        /* ── 404 giant number ── */
        .giant-404 {
            font-family: 'Sora', sans-serif;
            font-size: clamp(5rem, 18vw, 8.5rem);
            font-weight: 800;
            line-height: 1;
            letter-spacing: -4px;
            background: linear-gradient(135deg, var(--cyan) 0%, var(--blue) 55%, var(--purple) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
            display: inline-block;
            animation: fadeUp 0.5s 0.1s both;
            filter: drop-shadow(0 4px 18px rgba(6,182,200,0.22));
        }
            /* Stethoscope hanging on the 0 */
            .giant-404 .badge-steth {
                position: absolute;
                top: -8px;
                right: -22px;
                width: 38px;
                height: 38px;
                background: linear-gradient(135deg, #fff, #e0f7fa);
                border-radius: 50%;
                border: 2px solid rgba(6,182,200,0.25);
                display: flex;
                align-items: center;
                justify-content: center;
                box-shadow: 0 4px 12px rgba(6,100,130,0.15);
                animation: wiggle 3s ease-in-out infinite;
            }

        @@keyframes wiggle {
            0%, 100% {
                transform: rotate(-8deg);
            }

            50% {
                transform: rotate(8deg);
            }
        }

        /* ── Playful heading ── */
        .ooops-text {
            font-family: 'Sora', sans-serif;
            font-size: clamp(1.4rem, 5vw, 2rem);
            font-weight: 800;
            color: var(--text-dark);
            letter-spacing: -0.8px;
            line-height: 1.2;
            margin-bottom: 0.5rem;
            animation: fadeUp 0.5s 0.22s both;
        }

            .ooops-text .wave {
                display: inline-block;
                animation: wave 2s ease-in-out infinite;
                transform-origin: 70% 70%;
            }

        @@keyframes wave {
            0%, 100% {
                transform: rotate(0deg);
            }

            20% {
                transform: rotate(20deg);
            }

            40% {
                transform: rotate(-8deg);
            }

            60% {
                transform: rotate(14deg);
            }

            80% {
                transform: rotate(-4deg);
            }
        }

        /* ── Status strip ── */
        .status-strip {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 7px;
            background: rgba(124,58,237,0.07);
            border: 1px solid rgba(124,58,237,0.18);
            border-radius: 12px;
            padding: 8px 16px;
            margin: 1.2rem 0 1.4rem;
            font-size: 12.5px;
            color: #5b21b6;
            font-weight: 500;
            animation: fadeUp 0.5s 0.28s both;
        }

            .status-strip .mono {
                margin-left: auto;
                font-weight: 700;
                font-size: 11px;
                letter-spacing: 1px;
                color: var(--purple);
                font-family: monospace;
            }

        /* ── Sub texts ── */
        .sub-msg {
            font-size: 15px;
            font-weight: 500;
            color: var(--blue);
            margin-bottom: 0.55rem;
            animation: fadeUp 0.5s 0.32s both;
        }

        .desc-msg {
            font-size: 13.5px;
            line-height: 1.65;
            color: var(--gray-500);
            margin-bottom: 1.8rem;
            animation: fadeUp 0.5s 0.36s both;
        }

        /* ── Diagnostic cards ── */
        .diag-title {
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 1.2px;
            text-transform: uppercase;
            color: var(--gray-400);
            margin-bottom: 10px;
            text-align: left;
            animation: fadeUp 0.5s 0.40s both;
        }

        .diag-grid {
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            gap: 9px;
            margin-bottom: 1.8rem;
            animation: fadeUp 0.5s 0.43s both;
        }

        .diag-card {
            background: rgba(255,255,255,0.55);
            border: 1px solid rgba(6,182,200,0.14);
            border-radius: 12px;
            padding: 11px 12px;
            text-align: left;
        }

        .diag-icon {
            width: 28px;
            height: 28px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 7px;
        }

        .dc-cyan {
            background: var(--cyan-pale);
        }

        .dc-blue {
            background: var(--blue-soft);
        }

        .dc-purple {
            background: var(--purple-soft);
        }

        .dc-mint {
            background: var(--mint);
        }

        .diag-label {
            font-size: 10px;
            font-weight: 700;
            letter-spacing: 0.3px;
            color: var(--gray-400);
            text-transform: uppercase;
            margin-bottom: 2px;
        }

        .diag-val {
            font-size: 12px;
            font-weight: 600;
            color: var(--text-dark);
        }

        /* ── Divider ── */
        .soft-divide {
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(6,182,200,0.22), transparent);
            margin-bottom: 1.6rem;
            animation: fadeUp 0.5s 0.46s both;
        }

        /* ── URL path display ── */
        .url-strip {
            display: flex;
            align-items: center;
            gap: 8px;
            background: rgba(255,255,255,0.50);
            border: 1px solid rgba(6,182,200,0.16);
            border-radius: 10px;
            padding: 9px 14px;
            margin-bottom: 1.6rem;
            text-align: left;
            animation: fadeUp 0.5s 0.48s both;
        }

        .url-label {
            font-size: 11px;
            font-weight: 700;
            color: var(--gray-400);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            flex-shrink: 0;
        }

        .url-path {
            font-family: monospace;
            font-size: 12.5px;
            color: var(--text-mid);
            flex: 1;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .url-badge {
            flex-shrink: 0;
            background: rgba(229,62,62,0.10);
            border: 1px solid rgba(229,62,62,0.20);
            color: #b91c1c;
            font-size: 10px;
            font-weight: 700;
            padding: 2px 8px;
            border-radius: 8px;
        }

        /* ── Quick links ── */
        .ql-title {
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 1.2px;
            text-transform: uppercase;
            color: var(--gray-400);
            margin-bottom: 10px;
            text-align: left;
            animation: fadeUp 0.5s 0.50s both;
        }

        .ql-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 9px;
            margin-bottom: 1.8rem;
            animation: fadeUp 0.5s 0.52s both;
        }

        .ql-item {
            display: flex;
            align-items: center;
            gap: 10px;
            background: rgba(255,255,255,0.50);
            border: 1px solid rgba(6,182,200,0.13);
            border-radius: 11px;
            padding: 10px 13px;
            text-decoration: none;
            color: var(--text-mid);
            font-size: 13px;
            font-weight: 500;
            transition: all 0.2s ease;
        }

            .ql-item:hover {
                background: rgba(255,255,255,0.82);
                border-color: rgba(6,182,200,0.30);
                transform: translateY(-2px);
                box-shadow: 0 6px 18px rgba(6,100,130,0.09);
                color: var(--text-dark);
            }

        .ql-dot {
            width: 7px;
            height: 7px;
            border-radius: 50%;
            flex-shrink: 0;
        }

        /* ── Buttons ── */
        .btn-row {
            display: flex;
            gap: 11px;
            animation: fadeUp 0.5s 0.55s both;
        }

        .btn-main {
            flex: 1;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background: linear-gradient(135deg, var(--cyan), var(--blue));
            color: white;
            font-family: 'Plus Jakarta Sans', sans-serif;
            font-size: 13.5px;
            font-weight: 600;
            padding: 13px 18px;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            text-decoration: none;
            box-shadow: 0 4px 16px rgba(6,182,200,0.30);
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

            .btn-main:hover {
                transform: translateY(-2px);
                box-shadow: 0 8px 26px rgba(6,182,200,0.38);
                color: white;
            }

            .btn-main:active {
                transform: translateY(0);
            }

        .btn-alt {
            flex: 1;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            background: rgba(255,255,255,0.72);
            color: var(--text-mid);
            font-family: 'Plus Jakarta Sans', sans-serif;
            font-size: 13.5px;
            font-weight: 600;
            padding: 13px 18px;
            border: 1.5px solid rgba(6,182,200,0.20);
            border-radius: 12px;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.2s ease;
        }

            .btn-alt:hover {
                background: rgba(255,255,255,0.92);
                border-color: rgba(6,182,200,0.35);
                transform: translateY(-2px);
                box-shadow: 0 6px 18px rgba(6,100,130,0.09);
                color: var(--text-mid);
            }

            .btn-alt:active {
                transform: translateY(0);
            }

        /* ── Footer ── */
        .foot-note {
            margin-top: 1.4rem;
            font-size: 12px;
            color: var(--gray-400);
            text-align: center;
            animation: fadeUp 0.5s 0.60s both;
        }

            .foot-note a {
                color: var(--cyan-deep);
                font-weight: 500;
                text-decoration: none;
            }

                .foot-note a:hover {
                    text-decoration: underline;
                }

        /* ── Bottom bar ── */
        .bottom-bar {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            height: 42px;
            background: rgba(255,255,255,0.72);
            backdrop-filter: blur(14px);
            -webkit-backdrop-filter: blur(14px);
            border-top: 1px solid rgba(6,182,200,0.12);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 28px;
            z-index: 200;
        }

        .bar-item {
            display: flex;
            align-items: center;
            gap: 5px;
            font-size: 11px;
            color: var(--gray-400);
        }

        .bar-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
        }

            .bar-dot.g {
                background: #22c55e;
            }

            .bar-dot.r {
                background: #e53e3e;
                animation: blink 1.5s infinite;
            }

        @@keyframes fadeUp {
            from {
                opacity: 0;
                transform: translateY(13px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ── Responsive ── */
        @@media (max-width: 540px) {
            .card-wrap {
                padding: 1.8rem 1.3rem;
                border-radius: 20px;
            }

            .diag-grid {
                grid-template-columns: 1fr 1fr;
            }

            .ql-grid {
                grid-template-columns: 1fr;
            }

            .btn-row {
                flex-direction: column;
            }

            .tb-chip, .tb-pill {
                display: none;
            }

            .bottom-bar .bar-item:last-child {
                display: none;
            }
        }
    </style>
</head>
<body>

    <!-- Background -->
    <div class="bg-layer">
        <svg viewBox="0 0 1440 900" preserveAspectRatio="xMidYMid slice" xmlns="http://www.w3.org/2000/svg">
            <!-- Faint grid -->
            <defs>
                <pattern id="grid" width="44" height="44" patternUnits="userSpaceOnUse">
                    <path d="M44 0H0M0 0V44" fill="none" stroke="rgba(6,182,200,0.07)" stroke-width="0.8" />
                </pattern>
            </defs>
            <rect width="1440" height="900" fill="url(#grid)" />
            <!-- Soft arcs -->
            <circle cx="1380" cy="100" r="220" fill="none" stroke="rgba(6,182,200,0.07)" stroke-width="36" />
            <circle cx="60" cy="860" r="180" fill="none" stroke="rgba(37,99,168,0.06)" stroke-width="28" />
            <circle cx="720" cy="450" r="320" fill="none" stroke="rgba(124,58,237,0.04)" stroke-width="18" />
            <!-- Winding dashed ECG path -->
            <polyline points="0,580 120,580 150,580 175,460 200,650 230,420 265,580 380,580 410,580 435,510 460,620 495,540 530,580 800,580 1440,580"
                      fill="none" stroke="rgba(6,182,200,0.10)" stroke-width="1.8"
                      stroke-dasharray="6,6" stroke-linecap="round" stroke-linejoin="round" />
            <!-- Medical cross motifs -->
            <g opacity="0.07" stroke="#0891b2" stroke-width="1.5" stroke-linecap="round" fill="none">
                <line x1="80" y1="160" x2="80" y2="200" /><line x1="60" y1="180" x2="100" y2="180" />
                <line x1="1360" y1="680" x2="1360" y2="720" /><line x1="1340" y1="700" x2="1380" y2="700" />
                <line x1="680" y1="60" x2="680" y2="100" /><line x1="660" y1="80" x2="700" y2="80" />
                <line x1="1050" y1="200" x2="1050" y2="240" /><line x1="1030" y1="220" x2="1070" y2="220" />
                <line x1="280" y1="760" x2="280" y2="800" /><line x1="260" y1="780" x2="300" y2="780" />
            </g>
        </svg>
    </div>

    <!-- Orbs -->
    <div class="orb orb-1"></div>
    <div class="orb orb-2"></div>
    <div class="orb orb-3"></div>

    <!-- Floating medical icons -->
    <div class="float-items" id="floatItems"></div>

    <!-- Top bar -->
    <header class="topbar">
        <a href="#" class="tb-logo">
            <div class="tb-icon">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="white">
                    <path d="M9 3h6v6h6v6h-6v6H9v-6H3V9h6z" />
                </svg>
            </div>
            <span class="tb-wordmark">Hastane<em>Sis</em></span>
            <span class="tb-chip">HMS v4.2</span>
        </a>
        <div class="tb-right">
            <div class="tb-pill">
                <div class="tb-pill-dot"></div>
                Sayfa Bulunamadı
            </div>
        </div>
    </header>

    <!-- Main card -->
    <main class="card-wrap text-center">

        <!-- Giant 404 -->
        <div style="position:relative; display:inline-block; margin-bottom: 0.2rem;">
            <span class="giant-404">
                404
                <span class="badge-steth">
                    <!-- Mini stethoscope icon -->
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#0891b2" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M4.5 6.5A2 2 0 0 1 6.5 5h1A2 2 0 0 1 9.5 7v3a5 5 0 0 0 10 0V7a2 2 0 0 1 2-2" />
                        <circle cx="19.5" cy="5.5" r="1.5" />
                        <circle cx="12" cy="16" r="2" />
                    </svg>
                </span>
            </span>
        </div>

        <!-- Playful title -->
        <h1 class="ooops-text">
            Ooooopssss! Kayıp mı Oldunuz? <span class="wave">🧭</span>
        </h1>

        <!-- Status strip -->
        <div class="status-strip">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#5b21b6" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="11" cy="11" r="8" />
                <line x1="21" y1="21" x2="16.65" y2="16.65" />
            </svg>
            <span>Aradığınız kayıt sistemde mevcut değil veya taşınmış olabilir</span>
            <span class="mono">HTTP 404</span>
        </div>

        <!-- Sub texts -->
        <p class="sub-msg">Bu sayfa kaybolmuş, silinmiş ya da hiç var olmamış olabilir.</p>
        <p class="desc-msg">
            Endişelenmeyin — bu tür vakalar bizde de görülüyor. 😄<br>
            URL'yi kontrol edin ya da aşağıdaki bağlantılardan birini kullanın.
        </p>

        <!-- Diagnostic cards -->
        <p class="diag-title">🔍 Teşhis Raporu</p>
        <div class="diag-grid">
            <div class="diag-card">
                <div class="diag-icon dc-cyan">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#0891b2" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                        <polyline points="22 12 18 12 15 21 9 3 6 12 2 12" />
                    </svg>
                </div>
                <div class="diag-label">Durum</div>
                <div class="diag-val">Bulunamadı</div>
            </div>
            <div class="diag-card">
                <div class="diag-icon dc-blue">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#2563a8" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                        <circle cx="12" cy="12" r="10" />
                        <polyline points="12 6 12 12 16 14" />
                    </svg>
                </div>
                <div class="diag-label">Zaman</div>
                <div class="diag-val" id="diag-time">--:--</div>
            </div>
            <div class="diag-card">
                <div class="diag-icon dc-purple">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#7c3aed" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
                        <line x1="12" y1="9" x2="12" y2="13" />
                        <line x1="12" y1="17" x2="12.01" y2="17" />
                    </svg>
                </div>
                <div class="diag-label">Hata Kodu</div>
                <div class="diag-val">NOT_FOUND</div>
            </div>
        </div>

        <div class="soft-divide"></div>

        <!-- URL display -->
        <div class="url-strip">
            <span class="url-label">İstenen Yol</span>
            <span class="url-path" id="url-path">/hastane/bilinmeyen-sayfa</span>
            <span class="url-badge">Geçersiz</span>
        </div>

        <!-- Quick links -->
        <p class="ql-title">Hızlı Bağlantılar</p>
        <div class="ql-grid">
            <a href="/dashboard" class="ql-item">
                <div class="ql-dot" style="background:var(--cyan);"></div>
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7" /><rect x="14" y="3" width="7" height="7" /><rect x="14" y="14" width="7" height="7" /><rect x="3" y="14" width="7" height="7" /></svg>
                Kontrol Paneli
            </a>
            <a href="/patients" class="ql-item">
                <div class="ql-dot" style="background:var(--teal);"></div>
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" /><circle cx="9" cy="7" r="4" /><path d="M23 21v-2a4 4 0 0 0-3-3.87" /><path d="M16 3.13a4 4 0 0 1 0 7.75" /></svg>
                Hasta Listesi
            </a>
            <a href="/appointments" class="ql-item">
                <div class="ql-dot" style="background:var(--blue);"></div>
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2" /><line x1="16" y1="2" x2="16" y2="6" /><line x1="8" y1="2" x2="8" y2="6" /><line x1="3" y1="10" x2="21" y2="10" /></svg>
                Randevular
            </a>
            <a href="/reports" class="ql-item">
                <div class="ql-dot" style="background:var(--purple);"></div>
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="20" x2="18" y2="10" /><line x1="12" y1="20" x2="12" y2="4" /><line x1="6" y1="20" x2="6" y2="14" /></svg>
                Raporlar
            </a>
        </div>

        <!-- Buttons -->
        <div class="btn-row">
            <a href="/" class="btn-main">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z" />
                    <polyline points="9 22 9 12 15 12 15 22" />
                </svg>
                Ana Sayfaya Dön
            </a>
            <a href="javascript:history.back()" class="btn-alt">
                <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                    <polyline points="15 18 9 12 15 6" />
                </svg>
                Geri Git
            </a>
        </div>

        <p class="foot-note">
            Sorun devam ediyorsa
            <a href="mailto:destek@hastanesis.com">destek@hastanesis.com</a>
            ile iletişime geçin.
        </p>

    </main>

    <!-- Bottom bar -->
    <div class="bottom-bar">
        <div class="bar-item"><div class="bar-dot g"></div>Sistem Aktif</div>
        <div class="bar-item"><div class="bar-dot r"></div>Sayfa Bulunamadı</div>
        <div class="bar-item" id="bar-time"></div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        /* ── Show current URL path ── */
        (function () {
            const el = document.getElementById('url-path');
            if (el) el.textContent = window.location.pathname || '/hastane/bilinmeyen-sayfa';
        })();

        /* ── Live clock ── */
        function tick() {
            const now = new Date();
            const fmt = now.toLocaleString('tr-TR', {
                day: '2-digit', month: '2-digit', year: 'numeric',
                hour: '2-digit', minute: '2-digit', second: '2-digit'
            });
            const bt = document.getElementById('bar-time');
            if (bt) bt.textContent = fmt;
            const dt = document.getElementById('diag-time');
            if (dt) dt.textContent = now.toLocaleTimeString('tr-TR', { hour: '2-digit', minute: '2-digit' });
        }
        tick(); setInterval(tick, 1000);

        /* ── Floating medical emoji items ── */
        (function () {
            const icons = ['🩺', '💊', '🩹', '🧬', '💉', '🏥', '🩻', '❤️‍🩹', '🫀', '🧪'];
            const container = document.getElementById('floatItems');
            for (let i = 0; i < 14; i++) {
                const el = document.createElement('div');
                el.className = 'fi';
                el.textContent = icons[Math.floor(Math.random() * icons.length)];
                const size = 14 + Math.random() * 14;
                const dur = 12 + Math.random() * 14;
                const del = Math.random() * 12;
                Object.assign(el.style, {
                    left: (Math.random() * 100) + 'vw',
                    bottom: '-60px',
                    fontSize: size + 'px',
                    animationDuration: dur + 's',
                    animationDelay: del + 's',
                    opacity: '0'
                });
                container.appendChild(el);
            }
        })();

        /* ── Mouse parallax on orbs ── */
        document.addEventListener('mousemove', function (e) {
            const cx = window.innerWidth / 2, cy = window.innerHeight / 2;
            const dx = (e.clientX - cx) / cx, dy = (e.clientY - cy) / cy;
            document.querySelector('.orb-1').style.transform = `translate(${dx * 20}px,${dy * 14}px)`;
            document.querySelector('.orb-2').style.transform = `translate(${-dx * 16}px,${-dy * 11}px)`;
            document.querySelector('.orb-3').style.transform = `translate(${dx * 10}px,${-dy * 8}px)`;
        });

        /* ── Button ripple ── */
        document.querySelectorAll('.btn-main,.btn-alt').forEach(btn => {
            btn.addEventListener('click', function (e) {
                const r = document.createElement('span');
                const rect = btn.getBoundingClientRect();
                const size = Math.max(rect.width, rect.height);
                Object.assign(r.style, {
                    position: 'absolute', borderRadius: '50%',
                    width: size + 'px', height: size + 'px',
                    left: (e.clientX - rect.left - size / 2) + 'px',
                    top: (e.clientY - rect.top - size / 2) + 'px',
                    background: 'rgba(255,255,255,0.28)',
                    transform: 'scale(0)', animation: 'rpl .55s linear',
                    pointerEvents: 'none'
                });
                btn.style.position = 'relative';
                btn.style.overflow = 'hidden';
                btn.appendChild(r);
                setTimeout(() => r.remove(), 570);
            });
        });

        const s = document.createElement('style');
        s.textContent = '@@keyframes rpl { to { transform:scale(2.8); opacity:0; } }';
        document.head.appendChild(s);
    </script>
</body>
</html>